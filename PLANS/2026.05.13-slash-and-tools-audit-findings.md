# Slash Commands & Tool Calls — Audit Findings

**Status:** Phase 2 deliverable of the two-phase task started 2026-05-13.
Phase 1 produced
`/Users/rpeck/Source/claude-desktop-message-exporter/PLANS/2026.05.13-slash-and-tools-display-spec.md`
without reading implementation code. This Phase 2 audit reads the
implementation and reports divergences from the spec.

**No fixes are proposed in code in this document** — fixes are a
separate task per the user's instruction. This is spec-vs-code mapping
plus a prioritized fix list.

## Files audited

- `backend/claude_code_reader.py` (lines 1–1217)
- `backend/models.py` (lines 1–192)
- `backend/store.py:_parse_message` (lines 89–111)
- `backend/search.py` (lines 1–520) + `_extract_searchable_text`
- `backend/search_index.py` (slash_command projection note at L74)
- `backend/export.py` (lines 1–306)
- `frontend/src/lib/types.ts` (Message + ConversationDetail typing)
- `frontend/src/components/message/MessageBubble.tsx`
- `frontend/src/components/message/SlashCommandBadge.tsx`
- `frontend/src/components/conversation/SessionPreludeAffordance.tsx`
- `frontend/src/routes/ConversationPage.tsx` (prelude-toggle wiring)
- `backend/tests/test_local_command_triplet_collapse.py` (all 11 tests)
- `backend/tests/test_canned_response_fold_and_prelude.py` (all tests)
- `backend/tests/test_search_includes_slash_command.py`

---

## Audit summary — case-by-case

| # | Case | Verdict | Key evidence |
|---|------|---------|--------------|
| 1 | Argless slash command (`/exit`, `/clear`, …) | ⚠️ Partial | Backend correct; **export does NOT exclude argless markers** (`backend/export.py:302`); raw "Session: /exit" leaks into markdown bundles. |
| 2 | Argful slash command (`/plan <prose>`, …) | ❌ Diverges | **`is_command_marker=True` is set for BOTH argless and argful collapses** (`claude_code_reader.py:297`). Spec invariant X1 violated. The user's reported bug. |
| 3 | `/exit` + canned response fold | ✅ Matches spec | `_fold_canned_assistant_responses_into_marker` (cc_reader L377) implements the exact-match, role+content guard; tested bidirectionally at `test_canned_response_fold_and_prelude.py`. |
| 4 | Leading prelude markers | ✅ Matches spec | `_flag_leading_prelude_markers` (cc_reader L459) terminates on first non-marker OR argful marker; integration test covers the canonical 76fe578b shape. |
| 5 | Tool calls (`tool_use` / `tool_result`) | ⚠️ Partial | Search toggle gating works (`search.py:251-266` + `_extract_searchable_text` L46-142); viewer toggle works (`MessageBubble.tsx:510-517`). **But there's no `Expand/Collapse All Tools` toolbar control found in this audit** — UX.md §11 promises it; verify with grep before claiming. |
| 6 | Thinking blocks | ⚠️ Partial | Backend search projection handles `thinking` blocks (`search.py:135-140`). **Frontend `ContentBlockRenderer` (MessageBubble.tsx:499-534) has NO `case 'thinking':` branch** — thinking blocks are dropped silently from the viewer (default render path falls through to `return null`). |
| 7 | Compact markers (`type:"summary"`) | ✅ Matches spec | `extract_compact_markers` (cc_reader L543) and the parallel `compact_markers` list on `ConversationDetail` preserve the dumb-pipe contract. `/compact` triplet correctly skipped by the local-command collapser (test at L226 of `test_local_command_triplet_collapse.py`). |
| 8 | Attachments (multi-row fold) | ✅ Matches spec | Desktop attachments arrive on `Message.files`/`files_v2` and `attachments`; lightbox + dedupe via `dedupeImageFiles` (MessageBubble L65); export embeds via `_dedupe_image_files`. (Note: CC sessions don't have multi-row attachment folding — only Desktop JSON, which is built differently.) |

**Legend:** ✅ matches spec / ⚠️ partial divergence / ❌ outright divergence.

---

## Detailed divergences

### Case 2 (❌) — Argful slash commands carry `is_command_marker=True`

**Spec invariant X1 (violated):**
> `is_command_marker=True` AND `slash_command_args is not None and slash_command_args.strip() != ""` MUST NOT both be true.

**Code reality (`backend/claude_code_reader.py:297`):**
```
"is_command_marker": True,
```
This line is INSIDE `_collapse_local_command_triplets`, set unconditionally for every collapsed run regardless of whether the run is argless or argful. The collapser DOES already distinguish via the private `_argless_marker` flag (L311) for the prelude pass, but it does NOT use that distinction to gate `is_command_marker`.

**Consequence:**
- The `Message.is_command_marker` field is `True` on argful markers like `/coding Real prompt here.`. Any downstream consumer that reads `is_command_marker` as "this is a marker badge, suppress full bubble" would suppress the user's real prose.
- The frontend `MessageBubble` currently does NOT branch on `is_command_marker` to suppress the bubble — it renders the body unconditionally via the `slash_command` badge above (L204-206). So the user-visible damage is currently limited.
- BUT: any future feature (export filtering, search-result hiding, accessibility "skip markers" affordance) that reads `is_command_marker` will treat argful markers as noise. This is the user's "recent bug" pattern.

**Tests:** there is no test pinning that `is_command_marker == False` for argful markers. The existing `test_argful_command_marker_carries_args_as_text_and_slash_command` asserts the badge name and text body but does NOT assert `is_command_marker == False`. **Missing invariant test.**

**File:line evidence:** `backend/claude_code_reader.py:297`, plus
`test_local_command_triplet_collapse.py:393` (only asserts `is True`,
never asserts `is False` for argful).

---

### Case 1 / 4 (⚠️) — Export pipeline ignores `is_prelude` and `is_command_marker`

**Spec contracts (violated):**
- Case 1 "Export contract": argless markers excluded from markdown/PDF.
- Case 4 "Export contract": prelude markers excluded from all exports.

**Code reality (`backend/export.py:302`):**
```python
for message in conversation.messages:
    if message_has_visible_content(message, include_tools):
        lines.append(message_to_markdown(message, include_tools))
```
`message_has_visible_content` (L159-182) checks `text`, `content`,
images. It does NOT consult `is_prelude` or `is_command_marker`.

**Consequence:**
- Markdown bundle exports (CommonMark and Obsidian) include `"Session: /exit"` as a "You:" message at the top.
- Inline single-file Markdown exports leak the same.
- PDF export (via `conversation_to_html`, L309) inherits the same iteration; likely same leak (audit didn't read the full HTML pipeline, but it uses the same Message list).
- **`assistant_canned_response_consumed`** is also not consulted — exports include the marker but DON'T include the absorbed "No response requested." (because it was dropped by `_fold_canned_assistant_responses_into_marker` before export, which is fine).

**Tests:** no test asserts exports exclude markers/preludes. The
existing export tests (`test_export_bundle.py`, `test_export_pdf_images.py`) appear to focus on content correctness and image embedding, not marker filtering. **Missing test.**

**File:line evidence:** `backend/export.py:302`,
`backend/export.py:159-182` (visibility helper),
`backend/export.py:944` (PDF body renderer also iterates).

---

### Case 5 (⚠️) — "Expand / Collapse All Tools" control

**Spec / UX.md (article Part 2):** "an *'Expand / Collapse All Tools'* control that forces every tool block in the conversation open or closed at once. It only appears when the **Tools** toggle is on."

**Code reality:** the `expandAllTools` setting is consumed inside MessageBubble (`MessageBubble.tsx:41`, propagated to `ToolUseBlock`/`ToolResultBlock` via `forceExpanded`). There IS a per-bubble `setBubbleToolsCollapsed` toggle (L68). I did NOT see a conversation-header-level "Expand all / Collapse all" control during this audit.

**Action:** spot-check `frontend/src/components/conversation/` for the toolbar component. Marked partial pending verification. (Out of scope for this audit to confirm definitively; flag for fix-list triage.)

---

### Case 6 (⚠️) — Thinking blocks have no frontend renderer

**Spec contract:** "When shown: render as a distinct, muted collapsible block immediately preceding the main assistant text."

**Code reality (`MessageBubble.tsx:499-534`):**
```ts
switch (block.type) {
  case 'text': ...
  case 'tool_use': ...
  case 'tool_result': ...
  case 'image': ...
  default: return null
}
```
There is **no `case 'thinking':`**. Thinking content blocks fall through to `default: return null` and are silently dropped from the viewer.

**Consequence:**
- Backend `_extract_searchable_text` (search.py L135-140) DOES include thinking content in the FTS5 index when `include_tool_calls=True`. So a user searches for text inside a thinking block and gets a hit — but clicking the hit lands on a message that, in the viewer, shows NO thinking content (because it's not rendered). The result is a search "ghost" where the snippet says X but the bubble shows nothing matching.
- The viewer's "Tools" toggle currently has no impact on thinking display because thinking isn't rendered at all.

**Tests:** no frontend test pins the contract for thinking blocks. `test_search_includes_slash_command.py` doesn't cover thinking. **Missing invariant test on both sides.**

**File:line evidence:** `frontend/src/components/message/MessageBubble.tsx:499-534`.

---

### Case 5 / 6 (⚠️) — Search-toggle filter is per-message, not per-match

**Spec invariant X7 (refined):**
> A message M whose ONLY match site is inside `tool_calls`/`tool_results` MUST appear in search results when `showToolCalls=True` and MUST NOT when `showToolCalls=False`.

**Code reality (`backend/search.py:308-427`):**
The linear-scan path uses a cached `_extract_searchable_text` projection that toggles `include_tool_calls=False` to OMIT tool/thinking blocks from the haystack. Then `pattern.finditer(text)` runs on the filtered text. If `text` is empty after filtering, the message is skipped (L387-388: `if not text: continue`). This satisfies the spec invariant for the linear-scan path.

**FTS5 fast path (`backend/search.py:430-520`):** Step 1 calls `idx.query(...)` which returns matches based on the FULL projection (the index was built with `include_tool_calls=True`). The fast path then walks matched conversations and re-runs the snippet/regex loop with the toggle applied. **The re-run uses the SAME `_extract_searchable_text` with the user's `include_tool_calls` flag (L283-287)** — so the per-message regex on the projection IS toggle-aware, and a message whose match was only in tool content will produce no snippet hit and be dropped.

**Caveat (documented at search.py:259-266):** the FTS5 step 1 LIMIT 5000 may evict plain-text matches if 5000+ tool-only matches rank higher. **This is a known accepted drift.**

**Verdict:** the implementation matches the spec invariant in practice for typical corpora. The drift is documented and risk-accepted.

---

### The greedy `<command-args>` regex bug (confirmed)

**User's report:** for `/exit` markers dated 2026-04-21 and 2026-04-24 in session `76fe578b-…`, the marker text rendered as
`'</command-args>\n<command-name>/exit</command-name>...'`.

**Code (`backend/claude_code_reader.py:102`):**
```python
_LOCAL_CMD_ARGS_RE = re.compile(r"<command-args>(.*)</command-args>", re.DOTALL)
```

**Confirmed mechanism:** the regex is greedy (`(.*)`), single-shot, and `re.DOTALL`. The `_extract_local_command_name_and_args` function (L136-157) runs `_LOCAL_CMD_ARGS_RE.search(text)` on the raw row text. If a single CC JSONL row contains **multiple concatenated triplets** in its `message.content` string (e.g., two `<command-name>/exit</command-name>\n<command-args></command-args>` blocks glued together by a CC streaming-merge bug), the greedy match spans from the FIRST `<command-args>` to the LAST `</command-args>`. The captured args text includes the intermediate `</command-args>\n<command-name>/exit</command-name>` substring — exactly matching the user's observation.

**Why `_local_command_kind` (L105) doesn't catch it:** the kind classifier only checks `startswith("<command-name>")`. A multi-block row still starts with `<command-name>`, so it classifies as `command`. The collapser then extracts name+args from THE WHOLE ROW with the greedy regex.

**The existing test that "covers" this case (`test_marker_args_with_literal_close_tag_in_body_survives_greedy_regex` at `test_local_command_triplet_collapse.py:650-687`)** explicitly DESIGNS for the user-pasted single-block case ("I'm debugging a parser. </command-args>...") — it pins the greedy choice as intentional for that case. **It does NOT cover the multi-block concatenation case the user actually hit.**

**Test gap:** no test feeds a row whose text is two glued-together triplets and asserts the result is two separate parsed `(name, args)` tuples (or, fallback: that args is empty).

**Fix approach per Phase 1 spec § "The greedy regex bug":** replace single-shot greedy regex with a state-machine parser that bounds the args body to the next `<command-name>` tag if one appears before `</command-args>`. **Not implementing here per user instruction (Phase 3 says no fixes).**

---

### Plumbing: `_parse_message` forwards CC flags (`store.py:89-111`)

**Status:** the four CC-only fields ARE plumbed:
- `is_command_marker=raw.get("is_command_marker", False)` (L107)
- `is_prelude=raw.get("is_prelude", False)` (L108)
- `assistant_canned_response_consumed=raw.get("assistant_canned_response_consumed", False)` (L109)
- `slash_command=raw.get("slash_command")` (L110)

**Test that pins it:** `test_internal_argless_flag_does_not_leak_to_serialized_message` (`test_local_command_triplet_collapse.py:690-748`) calls `Message.model_validate(raw_marker)` and asserts `dumped["slash_command"] == "/exit"` AND `dumped["is_command_marker"] is True`. Also `test_pydantic_round_trip_preserves_flags` (`test_canned_response_fold_and_prelude.py:558-619`) round-trips through `ConversationStore.get_conversation` and asserts all four flags survive.

**User's claim "tests didn't catch the missing plumbing":** as of the current working tree this is **inaccurate** — the tests above DO catch it. However, both test files are **untracked** (not yet committed) so the test coverage and the fix appear to have been written together in the same uncommitted batch. Without the four lines added to `_parse_message`, both tests would fail at the `assert dumped["slash_command"]` step (because the default Pydantic `Message.slash_command` is `None`).

**What IS legitimately missing:** the four flags are duplicated in the `store.py:_parse_message` flat parse AND in the upstream `_merge_entries_to_message` / `_collapse_*` chain. There is no SHARED helper. If a fifth CC-only flag is added tomorrow, both call sites must be updated. **Plumbing fragility, not a bug today.**

---

## Cross-cutting invariants — audit

| Invariant | Status | Evidence |
|-----------|--------|----------|
| X1 (argful is never `is_command_marker`) | ❌ Violated | `claude_code_reader.py:297` |
| X2 (argful is never prelude) | ✅ Holds | `claude_code_reader.py:503` — `_flag_leading_prelude_markers` reads `_argless_marker` and breaks on False; tested at `test_local_command_triplet_collapse.py:543`. |
| X3 (no raw `<command-name>` leaks to content) | ✅ Holds for the standard path | tested at `test_local_command_triplet_collapse.py:142`. **Caveat:** the greedy-regex multi-block bug DOES violate X3 indirectly — the args body contains the raw substring, which then becomes the marker `text`. Marker `text` is exposed as `Message.text`. So X3 is **violated in the multi-block case**. |
| X4 (no `</command-args>` leaks) | ❌ Violated in multi-block case | Same mechanism as X3. The user's report is the direct evidence. |
| X5 (prelude is contiguous prefix) | ✅ Holds | `_flag_leading_prelude_markers` sets `in_prefix = False` once any non-marker is seen; tested at `test_canned_response_fold_and_prelude.py:376`. |
| X6 (no message-count drop around compact) | ✅ Holds | `extract_compact_markers` is read-only; the message list itself is untouched. Tested implicitly by `test_cc_compact_with_dup_uuids` fixture. **No explicit count-around-compact assertion test exists, however** — recommended addition. |
| X7 (search toggle gating) | ✅ Holds (with documented FTS5 LIMIT 5000 drift) | `_extract_searchable_text` honors `include_tool_calls`; both paths reuse it. |
| X8 (one truth, three surfaces) | ⚠️ Partial | Viewer and search honor the Tools toggle. Export DOES honor the toggle via `include_tools`. **But export does NOT honor `is_prelude`/`is_command_marker` exclusion** (see Case 1/4 finding above) — so the "three surfaces" agreement breaks for argless/prelude markers, not for tool calls. |

---

## Test coverage gaps

Test invariants that the spec demands but no current test pins:

1. **Argful marker has `is_command_marker == False`** — currently silent on this. Recommended: extend `test_argful_command_marker_carries_args_as_text_and_slash_command` with `assert marker.get("is_command_marker") is False`. (Note: this would FAIL today — that's the point.)
2. **Multi-triplet row produces no malformed args** — feed a row whose text is two glued triplets; assert the marker `text` does NOT contain the substring `</command-args>` or `<command-name>`. This pins the user's reported bug.
3. **Export excludes prelude markers** — pytest on `conversation_to_markdown` and `conversation_to_html`: build a CC `ConversationDetail` with a leading marker that has `is_prelude=True`; assert the marker text "Session: /exit" does NOT appear in the export.
4. **Export excludes argless markers** — same, but for a mid-conversation argless marker.
5. **Frontend renders thinking blocks (or explicitly hides them)** — Vitest on `ContentBlockRenderer`: pass a thinking content block; assert either it renders OR (if hidden) that there's a documented "Show thinking" affordance in the UI.
6. **Search-toggle X7 round-trip** — black-box test that builds a conversation with a tool-only match and a text-only match; asserts the search-toggle filter shows the right subset both ways.
7. **Compact-marker count invariant (X6)** — build a JSONL with N rows including K compact summary rows; assert `len(detail.messages) + len(detail.compact_markers) == N` (or document the exact count relationship).

---

## Implementation-fragility notes (not bugs, but at-risk)

- **Duplicate plumbing.** `_parse_message` (store.py:89) hand-copies four CC-only flags from `raw` to the Pydantic constructor. The CC-reader is the only producer of these flags, so a future producer addition (e.g., `is_thinking_present: bool`) must update both ends. A `**{k: raw.get(k, default) for k in CC_ONLY_FLAGS}` pattern would be safer but is a refactor, not a fix.
- **`_argless_marker` private flag.** Set in `_collapse_local_command_triplets` (L311) and read in `_flag_leading_prelude_markers` (L499). The Pydantic-default-`extra='ignore'` stripping is invariant — if a future Pydantic upgrade changes this default to `extra='allow'` or `extra='forbid'`, the private flag either leaks to the API or causes validation errors. Test at `test_local_command_triplet_collapse.py:740` pins the current behavior.
- **Search-index slash_command projection (search.py:96-98).** The slash command name is appended to the searchable text. The FTS5 tokenizer is `unicode61` which splits on `/`, so `/coding` indexes as the token `coding`. A query for the literal substring `/coding` works on the linear-scan path (regex match on raw text) but on the FTS5 path it depends on the prefix-search behavior. Documented but worth a black-box test.

---

## Prioritized fix list (top 5, ordered by user-visible impact)

| # | Fix | Why it matters | Effort |
|---|-----|----------------|--------|
| 1 | **Fix the multi-block `<command-args>` over-match** in `_extract_local_command_name_and_args` (`backend/claude_code_reader.py:102-157`). Replace the single greedy regex with a state-machine parser that terminates the args body at the next `<command-name>` tag if it appears before the next `</command-args>`. | The user is SEEING malformed `/exit` markers right now in session `76fe578b-…`. This is the most visible bug. | Small (1 function, 1 test) |
| 2 | **Stop setting `is_command_marker=True` for argful markers** in `_collapse_local_command_triplets` (`backend/claude_code_reader.py:297`). Change to `"is_command_marker": is_argless` so the flag tracks `_argless_marker` exactly. Add the missing invariant test. | Spec invariant X1; any future consumer of `is_command_marker` would treat argful markers as noise and risk hiding the user's prose. | Tiny (1 line + test) |
| 3 | **Exclude prelude markers AND argless markers from exports** in `backend/export.py:302` and the PDF path. Update `message_has_visible_content` or a sibling helper to consult `message.is_prelude` and `message.is_command_marker AND not message.slash_command_args`. | Markdown bundles and PDFs currently leak `"Session: /exit"` chrome. Article promises "one truth, three surfaces" but exports break that today. | Small (1-2 helpers + tests) |
| 4 | **Render thinking blocks in the viewer** OR explicitly document them as hidden in V1 + add the search-result-disappears warning to the article. Add `case 'thinking':` to `ContentBlockRenderer` (`MessageBubble.tsx:499`) gated by `showToolCalls` (or a new `showThinking` setting). | A user can search a thinking block, click the hit, and see a bubble that appears empty/wrong. Confusing UX. | Medium (new render branch + design call + tests) |
| 5 | **Add the missing invariant tests** (Test coverage gaps list above, items 1-7). These pin the spec at the contract layer and would catch any future regression in the fixes shipped above. | Without these tests, fixes 1-4 risk regressing on the next refactor. Bidirectional verification per `TESTING.md` §2. | Medium (~7 tests across pytest + vitest + playwright) |

---

## WWCMM (what would change this audit)

I would revise these findings if any of these become true:

1. **An `Expand/Collapse All Tools` toolbar control IS present** somewhere I didn't read (e.g., a new component under `frontend/src/components/conversation/` I missed). Verdict for Case 5 would shift from ⚠️ to ✅.
2. **The frontend DOES render thinking blocks** via a path I missed (e.g., `MarkdownRenderer` inside the `text` branch somehow consumes thinking). Verdict for Case 6 would shift to ✅, and the search-ghost concern dissolves.
3. **The greedy-regex multi-block evidence is actually a different bug** — e.g., the user's report from session 76fe578b's 2026-04-21/24 rows comes from a streaming-merge issue where two SEPARATE JSONL rows were merged into one `Message.text` by `_merge_entries_to_message` (which would be a different fix entirely). Verdict for the greedy-regex section would shift, and fix #1 would change shape.
4. **Argful-leading-prelude does NOT cause user-visible damage.** If MessageBubble or some other render path already checks `slash_command_args` presence and refuses to suppress argful bubbles, then the X1 violation is purely theoretical and fix #2 drops priority. (But invariant tests still recommended.)
5. **Export tests already exist** for prelude exclusion that I missed in this audit. (I searched on `is_prelude|is_command_marker` in `backend/export.py` and found no matches, but the export tests under `backend/tests/test_export_*.py` may pin it via integration-level fixtures.)
