# Plan: Backend test enhancement (frontend-spec-driven gap closure)

## Context

A research subagent just produced a frontend-derived API contract spec by
reading ONLY `frontend/src/**` — 24 endpoints, ~50 contract clauses,
explicit list of SSE event types, all `usePreferences` keys, the full
migration tombstone-keys story, and the image-fallback chain. The spec
reflects what the frontend EXPECTS (not what the backend DOES); that gap
is exactly the bug-hunting surface.

Inline audit of the existing 30 backend test files (~4476 LOC, ~155
tests) reveals five systemic discipline gaps relative to
`TESTING.md` §5:

1. **SSE: zero full-contract tests.** Greps for `aiter_lines`,
   `httpx.stream`, `event: start|progress|complete|capture_*`,
   `text/event-stream` — **all empty**. `/api/fetch/start` and
   `/api/fetch/refresh` are the most user-visible parts of the API
   (the sidebar Refresh button) and have effectively zero unit-test
   coverage of the stream contract. `test_refresh_pipeline.py` is
   network-bound and ignored by default per yesterday's test sweep.
2. **Concurrency: zero tests.** Greps for `asyncio.gather`,
   `asyncio.Lock`, `409 Conflict`, `race`, `concurrent` — empty.
   `_refresh_in_progress` lock is contract; untested. Atomic-write
   recovery untested.
3. **Security-adjacent: zero tests.** Greps for `0o600`, `st_mode`,
   `../`, `symlink`, `path_traversal` — empty. `/api/cc-image?path=...`
   and `/api/attachments/.../...` take paths-from-user-input;
   credentials.json + preferences.json should be `0o600`. Untested.
4. **Migration legacy seeds: zero tests.** Greps for `polarity:
   'include'|'exclude'`, `savedFilters: null`, `_migratedV1`,
   `_migratedV2` — empty in `backend/tests/`. The frontend orchestrates
   the migrations, but a seeded-legacy backend smoke would catch
   deserialization regressions and the backend's per-key-overwrite
   contract that nulls tombstone keys.
5. **Weak (status-only) tests: ~10 files** never examine the response
   body — `test_cc_image_watcher.py`, `test_claude_code_title.py`,
   `test_compact_markers.py`, `test_export_*.py` (most), `test_search_*.py`,
   `test_store_layout.py`. The grep for tests that don't reference
   `.json()`, `.content`, `.text`, or `content_type` returns 10
   files. Many of those tests would still pass if the response body
   were corrupted.

Plus endpoint coverage holes:

- `/api/conversations/<uuid>/tree` — no dedicated test file.
- `/api/orgs` 500-on-corrupt-credentials — only 5 tests in
  `test_orgs.py`; need to verify the 500 path the frontend treats as
  a distinct UI state.
- `/api/bookmarks` — only 5 tests; missing DELETE 404, PATCH 404,
  POST 409-on-duplicate.
- `/api/fetch/conversation/<uuid>` — 401/404/503 friendly-detail
  responses partially in `test_fetch_errors.py` but body shape
  undertested.
- `/api/fetch/status` — `credentials_age_days` null vs nonneg
  boundary not exercised.

This plan targets those gaps with discrete, parallelizable
deliverables. Each task ships a specific failure-mode coverage that
matches a frontend contract clause.

## Council review (2026-05-07)

Two councilors reviewed in parallel. Both returned APPROVE-WITH-CHANGES with mostly orthogonal additions and one preserved disagreement on P2 ordering. The plan below folds all agreed edits in. The disagreement is recorded with both arguments verbatim so downstream readers (including Ultraplan) have full argument context.

### Gemini-3-Pro · data-model + Python-backend seat

Load-bearing edits (accepted):

1. **Pydantic `model_config = ConfigDict(extra='forbid')`** retrofit on all input models. Argument: this is a self-contained local tool with no external clients; if the frontend sends unknown fields, we WANT a 422 to instantly catch drift. Highest-leverage single change to prevent future gap accumulation. Folded as new P6.
2. **Reprioritize P2** to Security → Migration → Atomic-Write → Concurrency → SSE. Argument: "Security and data integrity always trump feature bugs. A path traversal vuln or permission-bit drift is catastrophic; SSE event dropping is a sidebar UX glitch." Folded with the GPT compromise (see disagreement below).
3. **Colocate security tests in endpoint-specific files** (`test_cc_image.py`, `test_attachments.py`) rather than a generic `test_security_paths.py`. Argument: "Tests should document the endpoint's full contract; colocate failure modes with the feature." Folded — with GPT's carveout that shared helpers live in a small `backend/tests/_security_helpers.py`.

Bug bets Gemini will accept being wrong about:

- SSE event-order regressions: **1-3** (likely missing `cache-control: no-cache`, or failing to terminate on abort).
- Permission-bit drift on preferences.json: **1** (heavy bet that atomic temp-file creation inherits `0o644` from system umask instead of explicit `0o600`).
- Path-traversal vuln on `/api/cc-image`: **0** (FastAPI `Path` + `pathlib.resolve` usually handles it; still test it).
- Migration deserialization regressions: **1-3** (legacy models in strict Pydantic parsers almost always fail on edge cases).

Other Gemini positions:

- "Frontend may overstate contracts": when a test surfaces drift on status code (e.g., frontend assumes 201 on bookmark POST, backend returns 200), assert the BACKEND'S deterministic reality and update the spec — don't contort the backend to match a frontend assumption unless the HTTP semantics are genuinely wrong.
- Atomic-write recovery test scope: monkeypatch `os.replace` to raise; do NOT over-engineer kernel-level fsync races or cross-mount tmp scenarios. pytest is for Python-level exception boundaries, not kernel reordering.
- Migration backend scope: backend's role is "round-trip the legacy shape cleanly + accept explicit-null tombstone PATCH". Do NOT add a defensive normalizer that strips legacy keys silently — that masks bugs.
- `BACKEND-TEST-GAPS.md`: dated snapshot in `PLANS/`, not a living root-level doc. Use as burn-down chart for this plan, then let it die. `TESTING.md` is the living discipline doc.

### GPT-5.2 · UX + ergonomics + dev-experience seat

Load-bearing edits (accepted):

1. **Test naming standard:** `test__<endpoint>__<scenario>__<contract_outcome>` (double-underscore reads well in pytest output). Each new test gets a docstring citing the contract clause ID(s) from P1.1. Folded as a cross-cutting requirement on every new test.
2. **Spec-driven discipline header** on each new/rewritten test file:
   ```python
   # SPEC-DRIVEN TEST. Allowlist while authoring initial assertions:
   #   PLANS/2026.05.07-frontend-api-contract.md, TESTING.md,
   #   backend/models.py (types only), OpenAPI schema output.
   # Disallowed during initial pass: backend/routers/**, backend/services/**.
   # Second pass may consult routers ONLY to fix legitimate spec mismatches.
   ```
   Lightweight ritual, not enforced — but materially raises the bar for spec-vs-impl drift detection. Folded.
3. **Bidirectional verification at scale:** mandatory per-test for P2 (SSE/security/migration/atomic-write/concurrency); canary sampling (1 per file or per cluster) for P3 weak-upgrades since many P3 upgrades share the same falsification ("body corrupted but 200"). Folded into P5.2.
4. **`BACKEND-TEST-GAPS.md` guardrails:** every `✓` cell links to the exact test name + clause ID, plus a `Confidence` column (`high` = semantics + negative-space; `medium` = shape + some values; `low` = status + minimal). Prevents `✓ but low` from laundering weak asserts. Folded.
5. **Two-view gap matrix:** per-endpoint summary (24 endpoints, navigational) AND clause×test matrix (auditable detail). Folded.
6. **Pre-register ambiguities in P1.1** with columns: clause ID, observed backend behavior, frontend expectation, proposed resolution, owner, "needs council? Y/N". Default: only escalate to council on genuine product decisions (status-code taxonomy, error classes), not bugs. Folded.
7. **P0 conftest additions** (small, not a full overhaul): `isolated_data_dir`, `real_async_client`, `collect_sse_events(resp, *, stop_on=("complete","error"), timeout=...)`. Folded as new P0.
8. **Rich assertion messages required only on contract-class assertions** (event order/type, payload value/schema, negative-space). Not every assert — that's noise. Folded as a cross-cutting requirement.

Other GPT positions:

- "Two views" on the gap matrix is the right ergonomic surface; one giant cross-product table is unreadable; per-endpoint-only loses the clause×test traceability.
- For Pydantic `extra='forbid'`: agree with Gemini, but expect fallout. Suggest doing it first on `preferences/bookmarks/filter` inputs where frontend payloads are stable, before riskier endpoints.
- For test-failure triage cost: pre-register ambiguities upfront so council calls are reserved for genuine product decisions, not "just a bug".

### PRESERVED DISAGREEMENT — P2 ordering

> **Gemini's position:** strict serial Security → Migration → Atomic-Write → Concurrency → SSE. "A path traversal vuln or permission-bit drift is catastrophic; SSE event dropping is a sidebar UX glitch."
>
> **GPT's position (mild pushback):** "From UX-risk, SSE is the most user-visible (Refresh button); from safety-risk, security is. Test work IS parallelizable; security and SSE are both load-bearing; punishing SSE staffing for the order argument is a false economy. Keep Gemini's order for *merge-gating* and as the ranked priority for which fixes go first if multiple bugs surface — but don't block SSE work behind migration if staffing allows."

**Convergence (both councilors accepted in round 2):**

- Adopt Gemini's priority RANKING (Security > Migration > Atomic-Write > Concurrency > SSE) for: (a) which fix lands first when multiple bugs surface, (b) which task gets the senior reviewer, (c) merge-gating order if conflicts.
- Run task EXECUTION in parallel where capacity allows. Independent test files don't merge-conflict; SSE shouldn't be staffing-starved.
- Both councilors signed off:
  - Gemini: *"Priority-rank-but-parallel-staffing is a pragmatic resolution. Security and migration data-integrity regressions strictly block the merge queue and are prioritized for fixes ahead of SSE UX glitches; parallel test authoring is an acceptable economy of engineering resources."*
  - GPT: *"'Priority ranking without execution serialization' is still meaningful: it governs merge-gating, review attention, and bug-fix ordering when multiple failures surface at once. Meanwhile, parallel execution keeps time-to-signal low on SSE, which is user-visible and tends to surface surprising contract drift; delaying it doesn't make the system safer, it just delays discovery."*

The disagreement is recorded for downstream readers; the resolution is the compromise above.

## Goal

After this plan lands:

- Every contract clause in the frontend-derived spec has at least one
  strong-assertion backend test.
- Every SSE endpoint has a full event-order/types/payload/termination
  test using `httpx.AsyncClient` + `aiter_lines`.
- Every locked / atomic-op endpoint has a deterministic concurrency
  test.
- Every path-from-user-input endpoint has a path-traversal /
  symlink / extension-allowlist test.
- Every credential or preference file write asserts `0o600`.
- Every migration has a legacy-seed test that exercises the
  deserialization code path users will hit.
- Every weak status-only test is upgraded to assert the body
  contract.
- New `BACKEND-TEST-GAPS.md` ships as the persistent gap matrix; it
  becomes the spec for future test additions.

## Cross-cutting requirements (apply to every new/rewritten test)

Per the council:

- **Naming:** `test__<endpoint>__<scenario>__<contract_outcome>` (double-underscore separator). Example: `test__post_bookmarks__duplicate__returns_409`.
- **Docstring cites the contract clause ID(s)** from `PLANS/2026.05.07-frontend-api-contract.md` (the spec doc P1.1 produces). One line per clause; don't paste the full clause text.
- **Spec-driven discipline header** (the comment block above) at the top of every NEW test file. Existing files being strengthened in P3 don't need it retroactively but do gain a docstring citing the clause.
- **Rich assertion messages required on contract-class assertions** (event order/type, payload value/schema, negative-space). Not every `assert`. Example: `assert response.status_code == 201, f"POST /api/bookmarks expected 201 (Created) so the frontend can rely on body.id; got {response.status_code} ({response.text!r})"`.
- **Bidirectional verification:** P2 tests REQUIRE explicit revert-and-fail evidence in the commit message ("Verified test fails when X is reverted with message: ..."). P3 tests use canary sampling — one per file is sufficient.

## Phases (DAG)

Phases that touch disjoint files run in parallel. Each task is one
commit; multiple commits per phase OK. **Priority ranking** for
merge-gating + senior-reviewer assignment + bug-fix ordering follows
the council compromise: P0 → P2 (Security → Migration → Atomic-Write →
Concurrency → SSE) → P3 → P4 → P5 → P6. **Execution** parallelizes
where capacity allows; SSE work is not blocked behind migration.

### P0 · Conftest additions (foundation)

Small, scoped, lands first because P2 needs the helpers.

- `isolated_data_dir(tmp_path, monkeypatch)` fixture — sets
  `CLAUDE_EXPLORER_DATA_DIR`, calls `get_settings.cache_clear()`, and
  yields the path; clears the cache again on teardown. Per
  `TESTING.md` §5.1.
- `real_async_client(app)` fixture — `httpx.AsyncClient(transport=
  ASGITransport(app=app))` for SSE / async tests. The existing
  `client` (sync `TestClient`) stays for non-async tests.
- `collect_sse_events(resp, *, stop_on=("complete","error"), timeout=5.0)`
  helper — async generator wrapper that parses `event:`/`data:` lines
  and yields `(type, payload)` tuples until a `stop_on` event arrives
  or the timeout fires. Eliminates SSE-parser boilerplate across the
  P2.1 / P2.2 / P2.3 tests.
- `legacy_v1_prefs(isolated_data_dir)` fixture — writes a known v1
  `preferences.json` shape to disk; used by P2.8 / P2.9.

**Commit:** `test(conftest): isolated_data_dir, real_async_client, collect_sse_events, legacy_v1_prefs`

### P1 · Persist the frontend-derived spec + gap matrix

Independent. Run first; everything else references it.

- **P1.1** Persist the frontend-derived API contract spec as
  `PLANS/2026.05.07-frontend-api-contract.md` (the Explore agent's
  output verbatim, lightly edited for repo conventions). 24 endpoints,
  ~50 contract clauses, each clause assigned a **stable clause ID**
  (e.g., `BKM-201` for "POST /api/bookmarks returns 201 on creation")
  so test docstrings can cite them. Add an **"Ambiguities"** section
  per GPT's edit: each row has clause ID, observed backend behavior,
  frontend expectation, proposed resolution, owner, and "needs
  council? Y/N". Default: only escalate to council on genuine product
  decisions (status-code taxonomy, error classes), not bugs.
- **P1.2** Write `PLANS/2026.05.07-backend-test-gaps.md` (dated
  snapshot per Gemini, NOT a living root-level doc). Two-view
  structure per GPT:
  - **Per-endpoint summary** (24 endpoints): clauses, current
    coverage status (✓/partial/✗/n/a), risk band, "next tests" list.
    The maintainer-friendly navigational view.
  - **Clause × test matrix** (50 clauses × ~30 test files): every
    `✓` cell links to the exact `test__...` name AND the clause ID;
    every cell carries a **Confidence** column with values `high`
    (semantics + negative-space asserted), `medium` (shape + some
    values asserted), or `low` (status + minimal — flag for
    upgrade). Auditable detail view.
  - End with a Top-N gaps list ordered by user-visibility risk.

**Commit:** `docs(testing): persist frontend-derived API spec + backend gap matrix (per-endpoint + clause×test views)`

### P2 · High-impact gap closure

Council priority ranking (Security → Migration → Atomic-Write →
Concurrency → SSE) governs merge-gating, reviewer assignment, and
bug-fix order. EXECUTION parallelizes where capacity allows. Each
P2.x task is independent at the file level; run in parallel without
gating SSE behind security.

#### P2 priority tier 1 · Security (per Gemini: "data integrity > feature bugs")

- **P2.5 — `/api/cc-image` security.** Add to `backend/tests/test_cc_image.py` (NEW file, OR extend `test_files_proxy.py` if that's the existing home — confirm via P1.2). Per Gemini's colocation rule: tests live with the endpoint, not in a generic security file. Tests:
  (a) `?path=../../etc/passwd` → 403 or 400; body has `detail`; response content does NOT contain `/etc/passwd` bytes.
  (b) Symlink under `~/.claude/image-cache/` pointing to `/etc/passwd` → 404 or 403; no leak.
  (c) Extension allowlist: `?path=...foo.txt` → 400 (not a known image ext).
  (d) Encoded-slash variants (`%2F`) — assert canonicalize-and-resolve actually canonicalizes.
  Shared helpers (path builders, "assert refused without fs touch") in `backend/tests/_security_helpers.py`.
- **P2.6 — `/api/attachments/<conv>/<file>/<variant>` security.** Add to `backend/tests/test_attachments.py` (NEW or extend `test_desktop_attachments_full.py`). Tests:
  (a) `conv_uuid` or `file_uuid` containing `..` → 400; no fs touch.
  (b) `variant` outside the allowlist (`thumbnail|preview|original|document`) → 400.
- **P2.7 — Permission bits.** Extend `test_preferences.py` and (where credentials are written) `test_orgs.py` or new `test_credentials.py`:
  (a) After PATCH `/api/preferences`, `os.stat(prefs_path).st_mode & 0o777 == 0o600`.
  (b) After credentials write (mock the capture flow), same assertion on `credentials.json`.
  Per Gemini's bug bet: heavy chance the atomic temp-file inherits `0o644` from system umask instead of explicit `0o600` post-`os.replace`.

#### P2 priority tier 2 · Migration legacy seeds

- **P2.8 — Migration legacy seeds: v0→v1 (pinned/activeFilterIds → graph).** New `backend/tests/test_filter_migration.py`. Use `legacy_v1_prefs` fixture from P0. Tests:
  (a) Backend GET on legacy-shape prefs returns the seed unchanged (no error, no silent normalization).
  (b) PATCH `{savedFilters: null, activeFilterIds: null}` clears the legacy keys; subsequent GET shows them gone (per-key overwrite contract).
  (c) Per Gemini: backend MUST NOT defensively normalize legacy keys silently — that masks bugs.
- **P2.9 — Migration legacy seeds: v1→v2 (polarity → behavior).** Same file. Seed legacy v1 atom with `polarity: 'exclude'`. Backend GET returns the seed unchanged. Frontend orchestrates the actual transformation; backend's role is round-trip integrity.

#### P2 priority tier 3 · Atomic-write recovery

- **P2.4 — Atomic-write recovery.** Extend `test_preferences.py` and `test_bookmarks.py`. Per Gemini's scope discipline: monkeypatch `os.replace` to raise `OSError`; assert PATCH/POST raises (or returns 5xx with no leakage); assert original file is byte-identical pre/post; assert no `.tmp` leak. Do NOT over-engineer kernel-level fsync races or cross-mount tmp scenarios — pytest is for Python-level exception boundaries.

#### P2 priority tier 4 · Concurrency

- **P2.3 — Concurrency: `/api/fetch/refresh` lock.** New `backend/tests/test_fetch_concurrency.py`. Use `real_async_client` (P0) + `asyncio.gather(client.get(...), client.get(...))`. Assert one returns `200`, the other `409`. Use a deterministic barrier (e.g., monkeypatch the fetch generator to `await` an `asyncio.Event` mid-stream) to avoid sleep-based flakes.

#### P2 priority tier 5 · SSE event-order/types/payload/termination

- **P2.1 — SSE: `/api/fetch/start`.** New
  `backend/tests/test_fetch_start_sse.py`. Use
  `httpx.AsyncClient.stream(...)` + `aiter_lines()`. Assert event
  ORDER (`start` first, ≥1 `progress`, then `complete` or `error`),
  event TYPES from the frontend's enumerated list, PAYLOAD shape per
  event (`current`, `total`, `message`, `kind`/`retryable` for
  errors), termination (stream actually closes), and the
  `cache-control: no-cache` + `content-type: text/event-stream`
  headers. At least one happy-path test, one auth-failure test
  (asserting `kind: 'AUTH'`), one transient-failure test (asserting
  `kind: 'TRANSIENT'` + `retryable: true`).
- **P2.2 — SSE: `/api/fetch/refresh` capture phase.** New
  `backend/tests/test_fetch_refresh_sse.py`. Tests:
  (a) credentials present → no capture phase, goes straight to
      `start`/`progress`/`complete`.
  (b) credentials missing → `capture_start` → `capture_waiting_login`
      heartbeat → `capture_done` → fetch phase. Mock the
      Playwright capture path so the test runs offline.
  (c) capture failure → `capture_error` event with `kind` field.
  (d) post-capture fetch failure → final `error` event, no retry
      loop (per Build-9 spec: capture is run AT MOST ONCE per request).
- **P2.3 — Concurrency: `/api/fetch/refresh` lock.** New
  `backend/tests/test_fetch_concurrency.py`. Use
  `asyncio.gather(client.get(...), client.get(...))` with two
  concurrent requests; assert one returns `200`, the other `409`.
  Use a deterministic barrier (e.g., monkeypatch the fetch generator
  to await an `asyncio.Event` mid-stream) to avoid sleep-based flakes.
- **P2.4 — Atomic-write recovery.** Extend
  `backend/tests/test_preferences.py` and
  `backend/tests/test_bookmarks.py` with a "boom-during-rename"
  test: monkeypatch `os.replace` to raise `OSError`; assert the
  PATCH/POST raises (or returns 5xx with no leakage); assert the
  original file is byte-identical pre/post; assert no `.tmp` file
  leaked. Per `TESTING.md` §5.8 template.
_(P2.5–P2.9 moved up to priority tiers 1-2 per the council; see above.)_

### P3 · Strengthen weak tests (feature-clustered assignment, one-file-per-commit)

Per GPT's edit: when parallelizing across agents, assign by feature
cluster (exports together; search together; layout together) to
minimize fixture overlap. STILL land as one-file-per-commit so
bisects stay clean. Avoid mega-PRs touching 6 test files unless
necessary.

Each P3.x is one file → one commit upgrading status-only assertions
to body-contract assertions. Bidirectional verification uses canary
sampling (1 per file or per cluster), NOT per-test, since most P3
upgrades share the same falsification ("body corrupted but 200").

- **P3.1 — `test_cc_image_watcher.py`** — assert specific files
  copied (full path), file BYTES unchanged from source (no
  re-encoding), sha8 segment in destination filename matches
  `sha256(bytes)[:8]`.
- **P3.2 — `test_export.py` + `test_export_bundle.py` +
  `test_export_bundle_attachments.py`** — assert exact zip member
  list (sorted), content bytes for known fixture images, exact
  Markdown link rewriting (`[name](attachments/foo.pdf)` for
  CommonMark vs `[[attachments/foo.pdf]]` for Obsidian).
- **P3.3 — `test_export_pdf_images.py`** — already strong per
  `TESTING.md` §5.3 (byte signature against fixture PNG);
  verify still holds; add a multi-image conversation case.
- **P3.4 — `test_search.py` + `test_search_cache.py`** — assert
  exact match count, snippet `match_start`/`match_end` positions,
  ordering by `created_at` desc, group-by-conversation structure
  not a flat list. The frontend depends on the offsets; the test
  must too.
- **P3.5 — `test_search_tool_usage.py`** — assert tool-call
  snippets are/aren't surfaced per the frontend's expectation
  (the Cmd+G across conversations spec hits this).
- **P3.6 — `test_compact_markers.py` + `test_claude_code_title.py`
  + `test_store_layout.py`** — body assertions on every test that's
  currently status-only.

### P4 · Endpoint-coverage holes (parallelizable)

Each is one task → one new test file or one extended file.

- **P4.1 — `/api/conversations/<uuid>/tree`.** New
  `backend/tests/test_conversations_tree.py`. Build a fixture
  conversation with a branch (`parent_message_uuid` chain forks at
  some message). Assert `root_messages` is a tree (recursive
  `MessageNode` with `children`), `active_path` is an ordered list
  of UUIDs, the active path corresponds to the conversation's
  `current_leaf_message_uuid`. 404 on missing UUID.
- **P4.2 — `/api/orgs` 500-on-corrupt-credentials.** Extend
  `test_orgs.py`: write a corrupt `credentials.json` (e.g., truncated
  JSON, wrong shape), assert the GET returns 500 (the frontend
  treats this as a distinct "error banner" UI state per the spec).
- **P4.3 — `/api/bookmarks` error paths.** Extend
  `test_bookmarks.py`: DELETE on unknown id → 404 with `detail`;
  PATCH on unknown id → 404 with `detail`; POST with duplicate
  (conversation_id, message_uuid) → 409 (or whatever the spec
  says — confirm via P1.1 spec doc).
- **P4.4 — `/api/fetch/conversation/<uuid>` error-detail bodies.**
  Extend `test_fetch_errors.py` (or add `test_fetch_conversation.py`):
  401/404/503 each return a `detail` string the frontend can show
  in a toast. Assert the body has `detail`, the value is non-empty,
  and the status code matches the failure class.
- **P4.5 — `/api/fetch/status` boundary.** Extend `test_fetch_errors.py`:
  no credentials → `has_credentials: false`,
  `credentials_age_days: null`. Credentials present → non-negative
  integer. Credentials freshly written → `0`.
- **P4.6 — `/api/preferences` deep-merge stress.** Extend
  `test_preferences.py`: PATCH with multiple keys at once, assert
  per-key independence (changing key A doesn't touch keys B/C/D).
  PATCH with explicit `null` on key A doesn't clear keys B/C/D.
  PATCH with empty `data: {}` is a no-op.

### P5 · Verify + commit + update TESTING.md

- **P5.1** Run `DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib
  .venv/bin/pytest backend/tests -q`. Target: 100% green; total
  test count up by ~30-50 (current ~155 → target ~185-205).
- **P5.2** Bidirectional verification per `TESTING.md` §2,
  with the council's scaling rule:
  - **P2 tests (Security / Migration / Atomic-Write / Concurrency
    / SSE) — MANDATORY per-test verification.** For each, capture a
    deliberately-broken state (revert the contract surface) and
    confirm the test fails with an informative message. Then
    re-apply. Record the falsification in the commit body
    ("Verified test fails when X is reverted with message: …").
  - **P3 weak-upgrade tests — canary sampling.** One per file (or
    per feature cluster). Many P3 upgrades share the same
    falsification mode; per-test would be redundant.
  - **P4 endpoint-coverage tests — per-test verification** (these
    are new contract assertions, not upgrades).
- **P5.3** Update `TESTING.md` §7 reference-incidents
  table with any drift the new tests caught (one row per surfaced
  bug). Update `PLANS/2026.05.07-backend-test-gaps.md` to flip
  ✗→✓ on the gaps now closed; populate the `Confidence` column
  per GPT's guardrail.

### P6 · Pydantic `extra='forbid'` retrofit (Gemini's load-bearing)

Add `model_config = ConfigDict(extra='forbid')` to all input models
across `backend/models.py` and any module-local request models in
`backend/routers/`. Per Gemini: "highest-leverage single change to
prevent future gap accumulation." Per GPT's risk-scoping suggestion:

- **P6.1** Retrofit STABLE input shapes first: `preferences`,
  `bookmarks`, `filters`. These have established frontend payloads;
  drift surfacing here is signal, not noise.
- **P6.2** Retrofit lower-stability inputs: `fetch`, `cc-image`,
  `attachments`, `orgs`. May surface "frontend sends a deprecated
  field that's silently ignored" cases — fix the frontend OR the
  spec, don't loosen the model.
- **P6.3** For each retrofitted model, add a unit test:
  ```python
  def test__<model>__rejects_unknown_field(...):
      """<clause-id>: input model rejects unknown fields with 422."""
      r = client.patch("/api/<endpoint>", json={"data": {"<known_key>": "v",
                                                          "BAD_KEY_DOES_NOT_EXIST": 1}})
      assert r.status_code == 422, ...
      assert "BAD_KEY_DOES_NOT_EXIST" in r.text  # detail names the offending key
  ```

**Commit:** `refactor(models): forbid extra fields on input models + reject-unknown tests`

**Risk:** retrofitting may surface real frontend bugs that were
masked by silent field acceptance. Per Gemini's pre-emption: "if
GPT argues this is too aggressive: this is a local desktop UI
communicating with its own local backend via localhost. There is
no skewed client-server version matrix. We must fail fast." GPT
agreed but asked for stable-shape-first sequencing — reflected in
P6.1/P6.2 ordering above.

## Critical files

**New:**
- `PLANS/2026.05.07-frontend-api-contract.md` — frontend-derived
  spec, 24 endpoints, ~50 contract clauses with stable IDs +
  Ambiguities pre-registration table.
- `PLANS/2026.05.07-backend-test-gaps.md` — gap matrix as a dated
  snapshot per Gemini, two-view (per-endpoint summary + clause×test
  matrix with Confidence column) per GPT.
- `backend/tests/_security_helpers.py` — shared path-builders /
  "assert refused without fs touch" utilities.
- `backend/tests/test_fetch_start_sse.py`
- `backend/tests/test_fetch_refresh_sse.py`
- `backend/tests/test_fetch_concurrency.py`
- `backend/tests/test_filter_migration.py`
- `backend/tests/test_conversations_tree.py`
- `backend/tests/test_cc_image.py` (NEW, OR extend `test_files_proxy.py`
  if that's the existing home — confirm via P1.2 audit) — security
  tests colocated with the endpoint per Gemini's rule.
- `backend/tests/test_attachments.py` (NEW, OR extend
  `test_desktop_attachments_full.py`) — security tests colocated.

**Modified:**
- `backend/tests/test_preferences.py` (atomic write recovery,
  permission bits, deep-merge stress).
- `backend/tests/test_bookmarks.py` (atomic write, error paths).
- `backend/tests/test_orgs.py` (500 path).
- `backend/tests/test_fetch_errors.py` (status boundary, conversation
  error-detail bodies).
- `backend/tests/test_cc_image_watcher.py` + the export tests + the
  search tests + the store/layout tests (strengthen weak assertions).
- `TESTING.md` (§7 incident table, after P5.3).

**Reused:**
- `backend/tests/conftest.py` `client` and `temp_data_dir` fixtures
  (verify they support `tmp_path` + `monkeypatch.setenv` per
  `TESTING.md` §5.1).
- The frontend mock helpers in `frontend/e2e/fixtures.ts` for the
  contract reference (don't import; cross-check).

## Verification

End-to-end:

1. `cd /Users/rpeck/Source/claude-desktop-message-exporter && DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib .venv/bin/pytest backend/tests -q` → all green; new test count visible.
2. For SSE tests, run with `-W error::RuntimeWarning` to surface any "coroutine was never awaited" issues.
3. For each new test file, run `git revert` on the most-relevant contract surface in `backend/`, re-run that single file, confirm a useful failure message — then `git revert --abort`.
4. `BACKEND-TEST-GAPS.md` updated: every previously ✗ row now ✓ for the clauses we addressed.
5. Frontend Playwright tests still pass (no regression): `cd frontend && npx playwright test e2e/spec-filters-*.spec.ts e2e/manage-filters-*.spec.ts e2e/preferences-*.spec.ts --reporter=line`.

## Risks

- **SSE tests + asyncio + httpx**: the test must own the lifespan; use `httpx.AsyncClient(transport=ASGITransport(app=app))` rather than the synchronous `TestClient`. Set `asyncio_mode = "auto"` in `pyproject.toml` if not already set.
- **Concurrency test flakiness**: never use `asyncio.sleep` as a barrier. Use `asyncio.Event` set/cleared by a monkeypatched generator step. The test should be deterministic on a 1-core CI runner.
- **Path-traversal tests must not actually leak**: assert REJECT (4xx + no leak), don't assert content from outside the data dir on success — that would mean the impl is broken AND the test allows it. Stick to `assert response.status_code != 200` AND `assert "passwd" not in response.content` AND the underlying file system access never happened (mock or `tmp_path` to confirm).
- **Permission bits on macOS may surprise**: `os.umask` and the user's environment can affect computed bits. Ensure the impl explicitly `os.chmod(path, 0o600)` after `os.replace` and the test asserts the post-rename mode.
- **Migration tests need real `tmp_path`**: `lru_cache.cache_clear()` AND `monkeypatch.setenv("CLAUDE_EXPLORER_DATA_DIR", str(tmp_path))` per `TESTING.md` §5.1.
- **`BACKEND-TEST-GAPS.md` becomes a maintenance burden** if it lives forever. Plan for it: it's a snapshot from 2026-05-07; future agents update it ONLY when adding/removing endpoints or contract clauses, not on every test commit. Add a banner saying so.
- **The frontend-derived spec may overstate contracts** (frontend assumes things the backend doesn't actually promise — e.g., specific status codes, deterministic header values). When a test surfaces drift here, classify per `TESTING.md` §3 Phase-3 categories: violation / ambiguity / infra / spec-gap. Use the council to decide which.

## Out of scope

- Re-running the spec-driven Playwright sweep — it's already green.
- Rewriting the backend implementation. The plan adds tests; if a
  test surfaces a real backend bug, that's a separate fix.
- Performance / load testing.
- Cross-browser testing (covered separately by the Playwright suite).
- Frontend type-codegen from backend (would obviate the spec-drift
  class entirely; out of scope for this plan).
