# Cowork Local-Session Ingestion (refined)

## Implementation status (live dashboard)

| Phase | Status | Commit | Notes |
|---|---|---|---|
| 0 — agent_session_io seam | DONE | `fd60892` | 880 pass / 1 skip (baseline). Transient-break verified via commented re-export → ImportError → restore. |
| 1 — cowork_reader + tests | DONE | `cb09742` | 895 pass (+15 new). Transient-break verified via commented `_audit_timestamp` mapping → fallback to `datetime.now()` → RED → restore. Live shape verified against the maintainer's real `~/Library/Application Support/Claude/local-agent-mode-sessions/` data. |
| 2 — store wiring + source widening | DONE | `6b5ea24` | 899 pass (+4 new). 22 source-literal sites widened. Detail-route resolver Cowork branch added. Autouse conftest fixture isolates `CLAUDE_DESKTOP_APP_DIR` for every test (caught: 25 pre-existing tests would have leaked real Cowork data). Transient-break: comment out cowork branch in `_get_all_conversations_data` → `expected 4, got 2` → restore. |
| 3 — search-index integration | DONE | `75c36ae` | 902 pass (+3 new). `_drift_first_scan` return-shape widened to `list[tuple[Path, str]]` (updated 4 existing tests). Source-tag dispatch replaces extension-based — Cowork's audit.jsonl now correctly routes through `read_cowork_conversation`. Transient-break: revert to extension dispatch → CC reader called on Cowork path → wrong-uuid index entries. |
| 4 — watcher second Observer | DONE | `d529892` | 905 pass (+3 new). Mirrors projects Observer with audit.jsonl-basename filter; cleanup in both cancellation + cooperative-shutdown branches. Transient-break: disable `_try_start_cowork_observer()` call → `'cowork' not in captured` → restore. |
| 5 — frontend source filter | DONE | `edeac94` | 409 vitest pass. Cowork spec: 2 pass. Sparkles icon in purple-500. data-testid='source-filter-select' added. Group-by-project toggle excluded under Cowork filter. Transient-break: remove the new SelectItem → both e2e RED → restore. |
| 6 — UX polish (D8-D11) | DONE | `0464126` | 912 pass (+7 backend new); 6 Cowork e2e pass. D8 archived hide + sidebar toggle (visible only under CLAUDE_COWORK/all); D9 yellow alert banner; D10 "Sandbox path" label. D11 done at Phase 1. Transient-break: disable archive filter → `test_default_request_hides_archived` RED → restore. Used `.click()` rather than `.check()/.uncheck()` for the controlled checkbox so React's commit window is permissive. |
| 7 — Time Machine restore extension | DONE | `de33c70` | 916 pass (+4 new). `domain_home_suffix` / `domain_live_root` / `domain_allow_relpath` helpers. Cowork allow-list restricts restore to `audit.jsonl` + `local_*.json` sidecars. Transient-break: short-circuit allow-list → `garbage.txt` planned for restore → restore. |
| 8 — article updates | DONE | `3d552c9` | Three sources framing added to `articles/part_2_web_app.md` and `_userdoc.md` twin. Source-dropdown listing widened in all 3 places. `PLANS/medium-articles.md` already updated in Phase 1. |

## Context

Claude Desktop's "Cowork" tab is a Claude Code–style local agent for non-coding work — same harness, but sessions live entirely on disk under `~/Library/Application Support/Claude/local-agent-mode-sessions/<deployment>/<org>/`, never served by any API. The user reported "Cowork messages are missing"; the multi-org fetch path was a red herring. ~42 sessions / 134 MB / 5 months of the user's data are wholly invisible to Claude Explorer today.

Outcome: Cowork sessions surface in the sidebar, are searchable, exportable, and filter-isolatable via the existing source selector — read-only, never writing to Claude Desktop's data dir. The Cowork `audit.jsonl` is the *same* JSONL shape the Claude Code reader already parses, so the work is mostly **reusing the 1400-LOC CC parser** plus **wiring a third `source` value through the stack**.

Scope confirmed with maintainer: **all phases** (core ingestion + UX polish + Time Machine restore-script extension + Medium docs). Path resolution: **add `platformdirs`** as a dependency.

---

## Corrections to the original draft (verified against the code)

1. **DROP the `thinking` schema change.** `backend/models.py:12` already types it as open `type: str`, and `frontend/src/lib/types.ts:120-129` *explicitly forbids* tightening it to a `Literal`. `thinking` blocks already round-trip and are silently dropped at render (`ContentBlockRenderer.tsx:106` `default: return null`) for **both** CC and Desktop today. Cowork inherits identical behavior for free. No schema change.

2. **Source-literal widening is ~22 sites, not 2.** Every `Literal["all","CLAUDE_AI","CLAUDE_CODE"]` / `Literal["CLAUDE_AI","CLAUDE_CODE"]` must gain `"CLAUDE_COWORK"`: `models.py` (×2: 120, 174), `search.py` (×5), `store.py` (×4), `search_index.py` (×8), `routers/conversations.py` (×1), `routers/search.py` (×2). Plus the frontend `SourceFilter` union + `SOURCE_FILTERS` array (`types.ts:332,344`).

3. **Detail-route resolver needs a Cowork branch — the draft omits this.** `store.py:_find_conversation_data` (≈line 629) resolves a clicked uuid → data for the detail/export endpoints. It only walks Desktop JSON + CC JSONL. Without a Cowork branch, clicking a Cowork row 404s. **Must-have.**

4. **Search-index dispatch collides on `.jsonl`.** `search_index.py:_load_conversation_at` (≈1769) dispatches purely on `path.suffix == ".jsonl"` → CC reader. Cowork's messages file is *also* `audit.jsonl`, so it'd be wrongly parsed by the CC reader. Fix: `_enumerate_conversation_paths` (≈1726) already returns `(path, source)` tuples — tag Cowork paths `"CLAUDE_COWORK"` and make `_load_conversation_at` dispatch on the **source tag** (thread it through), not the extension. Also `_resolve_path_for_conv` (≈1698) needs a Cowork branch for drift detection.

5. **`normalize_session_fields` must map MORE than `session_id`.** The shared transforms read `type`, `message.content`, `uuid`, `parentUuid`, `timestamp`, `sessionId`, `message.id`, `cwd`/`gitBranch`/`version`, `message.model`. The draft's helper only maps `session_id`→`sessionId` and **strips** `_audit_timestamp`. If Cowork lines carry `_audit_timestamp` but not `timestamp`, stripping makes every message fall back to `datetime.now()`. If lines lack `uuid`/`parentUuid`, `_get_message_key` collapses **all** user messages into one. → Map `_audit_timestamp`→`timestamp` (when `timestamp` absent); synthesize a stable `uuid` per line if absent; verify `parentUuid`. **See Phase 0a.**

6. **Restore script has no `RELATIVE_PATHS` array.** `utils/restore-deleted-sessions-and-images.sh` uses `DOMAINS=("projects" "image-cache")` and `resolve_domain_base_in_backup()` hardwires the `.claude/$domain` suffix (lines 459-465). Cowork lives under a **different root** (`Library/Application Support/Claude/...`). Needs a per-domain base-path mapping + an allow-list filter in the index walk. **See Phase 7.**

---

## Architecture: data flow + the five wiring points

```
   ~/Library/Application Support/Claude/local-agent-mode-sessions/
     <deployment>/<org>/ local_<uuid>.json   (sidecar: title, model, createdAt, isArchived, error, cwd)
                         local_<uuid>/audit.jsonl  (append-only messages)
                                  │
                                  ▼
        backend/cowork_reader.py  ──uses──►  backend/agent_session_io.py
        read_cowork_conversation()          (SHARED pure prims, extracted from cc_*)
        list_cowork_conversations()          parse_jsonl_file · _get_message_key
                                  │           _merge_entries_to_message · _get_message_text
          ConversationDetail dict │           normalize_session_fields(entry, format=)
          source="CLAUDE_COWORK"  │
        ┌─────────────────────────┴───────────────────────────────────┐
        ▼              ▼                    ▼               ▼            ▼
   (1) store         (2) store          (3) search_index (4) cc_watcher (5) frontend
   _get_all_         _find_             _enumerate_      2nd Observer    Sidebar
   conversations_    conversation_      conversation_    on cowork root  <SelectItem
   data()            data()             paths()  +       → _schedule_    "CLAUDE_COWORK">
   = SIDEBAR LIST    = DETAIL/EXPORT    _load_conv_at()  drift()         + SourceFilter
                                        = SEARCH/FTS5    = FRESHNESS     union widen
```

All five are gated by the `source` literal widening (correction #2). UUID dedup already in `_dedup_resume_continuations` is sufficient for cross-source collisions (Cowork UUIDs are disjoint per recon).

---

## Phase 0a — Schema verification (MANDATORY FIRST; runs on the user's Mac)

**Cannot be done in the planning/CI container — the data exists only on the user's machine.** Before writing the reader, dump a real line and confirm the field map:

```bash
DIR=~/Library/Application\ Support/Claude/local-agent-mode-sessions
head -3 "$(find "$DIR" -name audit.jsonl | head -1)" | python3 -m json.tool
cat "$(find "$DIR" -name 'local_*.json' | head -1)"   # sidecar shape
```

Confirm for each line: presence/name of `uuid`, `parentUuid`, `timestamp` vs `_audit_timestamp`, `session_id` vs `sessionId`, and that `message.content` matches the CC block shape. Record the verified map at the top of `cowork_reader.py`. Missing `uuid` → synthesize deterministically (e.g. `f"{session_id}:{line_index}"`).

---

## Phase 0b — Refactor seam: `backend/agent_session_io.py`

Move + re-export (keep old import paths working — pattern at `claude_code_reader.py:31-84`):

| Move to `agent_session_io.py` | From | Why safe |
|---|---|---|
| `parse_jsonl_file` | `cc_jsonl_io.py:57` | pure orjson line parse |
| `_get_message_text` | `cc_message_transforms.py:44` | walks `message.content` |
| `_get_message_key` | `cc_message_transforms.py:632` | groups by `type`/`uuid`/`message.id` |
| `_merge_entries_to_message` | `cc_message_transforms.py:649` | pure shape transform |
| `_convert_entry_to_message` | `cc_message_transforms.py:703` | pure shape transform |

**Stay CC-specific** (do NOT move): `_is_system_message`, `_title_from_entry`/`_TITLE_FIELD_BY_TYPE`, `read_conversation_summary_fast`, `_extract_conversation_metadata`, slash-command collapse / prelude / canned-fold passes.

```python
# backend/agent_session_io.py
def normalize_session_fields(entry: dict, *, fmt: Literal["claude_code", "cowork"]) -> dict:
    """Normalize a parsed JSONL line to the CC field shape the shared transforms expect.
    No-op for claude_code. For cowork: map session_id→sessionId, map _audit_timestamp→
    timestamp (only if timestamp absent), drop _audit_hmac. Exact map finalized in Phase 0a."""
    if fmt == "claude_code":
        return entry
    out = dict(entry)
    if "sessionId" not in out and "session_id" in out:
        out["sessionId"] = out["session_id"]
    if "timestamp" not in out and "_audit_timestamp" in out:
        out["timestamp"] = out["_audit_timestamp"]
    out.pop("_audit_hmac", None)
    # uuid synthesis / parentUuid handling per Phase 0a findings.
    return out
```

**Hard gate:** full existing CC suite (`test_cc_*.py`, `test_claude_code_title.py`, `test_cc_module_split.py`) must pass **unchanged**. Fallback if the physical move is churny: `cowork_reader.py` imports the pure functions directly from `cc_message_transforms` — `agent_session_io` is organizational, not load-bearing.

---

## Phase 1 — `cowork_reader.py` + path resolution + unit tests

**Path resolution** (`backend/config.py`, mirroring `claude_dir` at lines 183-285). Add `platformdirs` to `pyproject.toml`. Add `claude_desktop_app_dir: Path` to `Settings`, resolved: `CLAUDE_DESKTOP_APP_DIR` env → `config.json` `claude_desktop_app_dir` key → `platformdirs.user_data_path("Claude")` default. Cowork root = `claude_desktop_app_dir / "local-agent-mode-sessions"`.

**`backend/cowork_reader.py`** (≤100 LOC net-new — simpler than CC: no triplet collapse / prelude / canned passes):
- `read_cowork_conversation(session_dir: Path) -> dict | None` — `parse_jsonl_file` → per line `normalize_session_fields(.., fmt="cowork")` → group by `_get_message_key` → merge via `_merge_entries_to_message`. Pair with sidecar `local_<uuid>.json` for title/model/createdAt/isArchived/error/cwd; **tolerate missing/corrupt sidecar** → title="Untitled". Stamp `source: "CLAUDE_COWORK"`. Skip sessions with no `user` record (D6). Keep `unparsed_events` list for unknown types (log+count+skip+preserve).
- `list_cowork_conversations() -> list[dict]` — walk `<deployment>/<org>/local_*/`, flatten deployment layer (D2), parallel-read.

**Resilience:** partial-last-line — if the **last** line fails `JSONDecodeError` and file doesn't end in `\n`, drop that line only, don't mark corrupt. Truncate any single content payload > 1 MB with `[truncated; N bytes]` (D11).

**Same-commit doc anchors:** persist plan to `PLANS/2026.05.25-cowork-support.md` + add Cowork row to `PLANS/medium-articles.md`.

**Tests** `backend/tests/test_cowork_reader.py` + fixtures `backend/tests/fixtures/cowork/<deployment>/<org>/local_X/audit.jsonl` (RED-first): one-record-per-type, empty (no user → dropped), partial-last-line (±trailing `\n`), missing-sidecar, unknown-type, thinking-block validates, 2 MB payload truncates, `_audit_hmac` stripped.

---

## Phase 2 — Backend store wiring + source-literal widening

- Widen all `source` literals (correction #2) to include `"CLAUDE_COWORK"`.
- Plumb cowork root onto `ConversationStore.__init__` (`store.py:322`) like `claude_dir`.
- `store._get_all_conversations_data` (`store.py:505`): third branch `if source in ("all","CLAUDE_COWORK"): conversations.extend(list_cowork_conversations(...))`.
- `store._find_conversation_data` (`store.py:≈629`): Cowork resolution branch (correction #3).
- `_build_detail_from_data` (`store.py:728`): extend the `is_cc` chronological-stream guard to `source in ("CLAUDE_CODE","CLAUDE_COWORK")`.
- **Integration test:** mixed corpus; `source="all"` returns all three; `"CLAUDE_COWORK"` returns only Cowork; dedup distinct; archived hidden by default / shown with `show_archived=true`.

---

## Phase 3 — Search-index integration

- Widen `source` literals across `search_index.py` (×8) + `search.py` (×5) + `routers/search.py` (×2).
- `_enumerate_conversation_paths` (`search_index.py:1726`): append Cowork `audit.jsonl` paths tagged `"CLAUDE_COWORK"`.
- `_load_conversation_at` (`search_index.py:1752`): **dispatch on source tag, not extension** (correction #4) — route `"CLAUDE_COWORK"` → `read_cowork_conversation`.
- `_resolve_path_for_conv` (`search_index.py:1698`): Cowork branch.
- **Search integration test:** index a Cowork fixture, assert a known token returns it via the FTS5 fast path.

---

## Phase 4 — Watcher second Observer

Mirror the projects-dir Observer block in `cc_watcher.py` (`_build_projects_event_handler` 511, `_try_start_projects_observer` 549, `shutdown_projects_drift` 598). Add `_live_cowork_root()`, `_build_cowork_event_handler` (fires shared `_schedule_drift(path)` on `audit.jsonl` changes), `_try_start_cowork_observer` started alongside the others in `run_watcher`. Shared `_fire_drift_pass` → `update_drifted_files` re-enumerates via Phase 3, so freshness is automatic. Test `test_cowork_watcher.py`: drop a new session, assert index gains its tokens within ~5 s.

---

## Phase 5 — Frontend source filter

- `types.ts:332,344`: add `'CLAUDE_COWORK'` to `SourceFilter` and `SOURCE_FILTERS`.
- `Sidebar.tsx:213`: add `<SelectItem value="CLAUDE_COWORK">` (icon + "Claude Cowork") after the Claude Code item. Default visible.
- e2e `frontend/e2e/cowork-sidebar.spec.ts`: mock 2 Cowork sessions; filter shows "Claude Cowork"; selecting filters; console-assertion fixture (TESTING.md §5.15).

---

## Phase 6 — UX polish (D8–D11)

- **D8** archived hide + toggle: read `isArchived` from sidecar; default-hide; `show_archived` query param (store + router) + sidebar toggle.
- **D9** error banner: surface sidecar `error` (when non-null) in detail view.
- **D10** sandbox path: render `cwd` (`/sessions/<vm>`) as plain text labeled "Sandbox path"; no host-FS linking. (Sidebar "group by project" toggle at `Sidebar.tsx:284` keys off `sourceFilter !== 'CLAUDE_AI'` — confirm Cowork groups sanely or exclude it.)
- **D11** truncation already enforced at ingest in Phase 1.

---

## Phase 7 — Time Machine restore-script extension

`utils/restore-deleted-sessions-and-images.sh`. No `RELATIVE_PATHS` array exists (correction #6). Add a parallel base-path map: `projects`/`image-cache` → `.claude/$domain`, new `cowork` → `Library/Application Support/Claude/local-agent-mode-sessions`. Generalize `resolve_domain_base_in_backup()` (lines 455-487) to take the per-domain suffix. In the index walk (`index_domain`, line 508), add an **allow-list filter** for cowork: restore only `audit.jsonl` + sidecar `local_<uuid>.json`; skip `outputs/`, `uploads/`, `shim-lib/`, `shim-perm/`, `.claude/`, `.audit-key`. Preserve existing semantics (latest-snapshot-wins, never overwrite live, dry-run default, macOS-only — document in header). Test: synthetic snapshot under `tmp_path` with a missing Cowork pair + a missing CC `.jsonl` (regression guard); `--dry-run` lists both; `--apply` restores both; second `--apply` is a no-op.

---

## Phase 8 — Article updates

`articles/part_2_web_app.md` + twin `part_2_web_app_userdoc.md`: extend the "sources" framing to **three** (Desktop, Claude Code, Cowork); add a short "Claude Cowork sessions" section; update the source-filter screenshot. Optional Part 7 beat: the shared `agent_session_io` refactor prevented duplicating the 1400-LOC CC reader.

---

## Out of scope

HMAC verification (D4); serving `outputs/`/`uploads/` artifacts; displaying `enabledMcpTools`/`remoteMcpServersConfig` (D5 — strip on ingest); editing/deleting Cowork sessions (read-only); rendering `thinking` blocks (CC-parity — dropped at render, correction #1).

---

## Verification (manual UAT, on the user's Mac)

1. Restart `claude-explorer serve`; lifespan log reports loaded Cowork count.
2. Sidebar → "Claude Cowork": sessions appear, titled per sidecar JSONs.
3. Search a known term → ≥1 Cowork hit; click → detail renders, no `_audit_hmac` leak.
4. Export one Cowork session to Markdown — content survives.
5. Archive a session in Desktop, refresh → disappears; toggle "Show archived" → reappears (D8).

## Phase / PR order

`0a (verify) → 0b (seam, CC tests green) → 1 (reader) → 2 (store) → 3 (search) → 4 (watcher) → 5 (frontend) → 6 (polish) → 7 (restore) → 8 (docs)`. One PR per phase; 0b before 1 so the DRY seam is enforced by the passing CC suite before any Cowork code exists.

---

That's the full plan inline. Copy it into your local CLI and you're set to implement there.
