# Medium-docs update plan — topics since Cowork shipped (2026-05-26)

**Status:** handoff plan. A fresh session executes this plan to update
the Medium article series (primarily Part 2 long-form +
user-doc twin) to reflect every user-visible change landed since the
last Medium docs commit `3d552c9` on 2026-05-25.

**Supersedes:** [`PLANS/articles/__archive/2026.05.25-medium-docs-cowork-followup.md`](__archive/2026.05.25-medium-docs-cowork-followup.md)
(Cowork-only follow-up). All still-relevant Cowork to-dos from that
plan are folded into Topic 8 below. Do NOT delete the older plan —
it carries the original UAT checklist and screenshot specifics that
remain authoritative.

**Owning context:**
- Series outline + Confirmed Decisions table:
  [`PLANS/articles/medium-articles.md`](articles/medium-articles.md)
- Long-form article: [`articles/part_2_web_app.md`](../articles/part_2_web_app.md)
  (~616 lines; the deep-dive twin)
- User-doc twin: [`articles/part_2_web_app_userdoc.md`](../articles/part_2_web_app_userdoc.md)
  (~305 lines; product-tour cut)
- Cross-refs in Parts 1, 3, 6, 7 as flagged per-topic below.

**Commit range covered:** `3d552c9..HEAD` (13 commits, all unpushed
on `main`). Grouped by user-visible surface below.

**Do NOT edit any article file inside this plan.** This is a planning
artifact. Edits happen in another session. The plan's job is to be
specific enough that the next session can grep + locate + edit
without re-deriving context.

---

## How to use this plan

Each topic below carries six fields:

1. **Topic** — one sentence as a reader of the article would see it.
2. **Article files + sections to edit** — file path, line anchor or
   section heading, quoted prose that needs to change or be added.
3. **Suggested framing** — the angle to take in prose. Not the prose
   itself; just the mental model so the next session writes article
   voice, not changelog text.
4. **Screenshots** — what's stale, what to add, what state to set up
   to capture each.
5. **User-doc twin sync** — whether the parallel passage in
   `part_2_web_app_userdoc.md` needs a matching update, a lighter
   mention, or a skip.
6. **Cross-refs** — Parts 1, 3, 6, 7 references the next session
   should add. Default is "none" — only call out when there's a real
   cross-link.

After all topics, two final sections cover:
- **Confirmed-decisions table updates** for `PLANS/articles/medium-articles.md`.
- **Verification checklist** — diff + grep helpers, plus the still-
  pending Cowork UAT inherited from the superseded plan.

---

## Topic 1 — Show Compactions toggle (filter, not destruction)

### Article files + sections to edit

**`articles/part_2_web_app.md`:**

- Section: `## Reading Messages` → `### Tool blocks and slash commands`
  (lines 163–179). The current prose covers the Tools toggle and
  Expand/Collapse-All. Add a parallel paragraph (or sub-section
  `### Show Compactions toggle`) immediately AFTER the existing
  Expand/Collapse-All paragraph on line 179. Place is right — both
  toggles live on the conversation header; both are filters.
- Section: `### Scope composition` (lines 250–254). The current
  paragraph enumerates: source dropdown, workspace dropdown, active
  filter, and the **Tools** toggle. After "Search results also
  respect the **Tools** toggle in the conversation header, so a hit
  you couldn't see in the viewer never shows up in the result list
  either" (line 252) add a parallel sentence for Show Compactions:
  the same scope semantics apply.
- Section: `## Inside the Conversation Pane` → consider adding a
  sentence in the `### Copy, branches, and scroll-to-match` block
  (lines 441–447) noting that the **unified compact toggle** (see
  Topic 7) carries through to copy / export the same way as the
  Tools toggle does (the existing "one truth, three surfaces" beat
  on line 443 extends naturally).

**`articles/part_2_web_app_userdoc.md`:**

- Section: `### Tool blocks and slash commands` (lines 133–143). After
  the "Expand/Collapse All" paragraph (line 143), add a parallel
  short paragraph for Show Compactions. Keep it product-tour terse:
  what the checkbox does, when to use it.
- Section: `#### Scope composition` (lines 200–202). Add a clause
  noting Show Compactions also narrows search results.

### Suggested framing

Frame Show Compactions as a **filter, not a destructive operation**.
The conversation isn't modified; only the rendering hides/reveals.
The user's mental model the commits codified: "the toggle should
affect everything I see, including search results — and it does
now." This was a multi-source bug-hunt: compaction detection runs
through (a) CC's `isCompactSummary` field, (b) CC text-prefix
fallback for legacy sessions, (c) Cowork text-prefix detection
(no `isCompactSummary` field exists in Cowork). The title-derived
case ("Continuation of …") is also filtered. The user-visible
upshot is one checkbox that hides compactions everywhere, not just
in the viewer.

Avoid jargon ("schema v14 migration", "FTS5 secondary column"). Save
schema-bump narrative for Part 7 (perf postmortem) or the
LLM-Council Part 6 bug-hunt prose where it fits the "behind the
scenes" beat.

Receipts: commit `7832b86` (squashed 8-commit Round-4 cleanup).

### Screenshots

- **Stale:** `Pasted image 20260515191033.png` (line 171) shows the
  Tools toggle in pre-checkbox button form AND lacks any Show
  Compactions affordance. This needs replacement (see also Topic 5).
- **New:** the replacement header screenshot should show BOTH
  checkboxes side by side: `[ ] Show Tools` and `[ ] Show
  Compactions`, with a `/compact` summary card visible just below
  the header to anchor the reader. State to set up: open a CC
  session that contains at least one `/compact` boundary (any of
  the user's archived CC sessions where `/compact` was used works).
  Use the project's existing CC-with-compact fixture if you don't
  have a live session handy.
- **Optional second screenshot:** sidebar header showing the same
  conversation BEFORE clicking Show Compactions (compaction card
  visible) and AFTER (compaction card hidden, canonical-first-
  message title restored in the conversation list row above).
  Use a two-up `side-by-side` image; one comparison shot is more
  efficient than two separate captures.

### User-doc twin sync

Yes — parallel passage in the user-doc twin is required (under
`### Tool blocks and slash commands`). Use a SHORTER framing than
the long-form: one sentence on what the checkbox does, one on
search-scope behavior. Skip the multi-source detection story; it's
deep-dive material that belongs in the long-form twin.

### Cross-refs

- Part 7 (perf postmortem): the schema v13 → v14 migration story is
  a perfect "self-healing migration race-fix" beat for the perf
  postmortem's third act ("schema bumps that didn't break the live
  user mid-restart"). Add to TODO list for Part 7 plan in
  `PLANS/articles/part7_perf_postmortem_plan.md` (currently empty).

---

## Topic 2 — Demonstrated-focus arbitration (least-surprise UX)

### Article files + sections to edit

**`articles/part_2_web_app.md`:**

- Section: `### The three-pane focus model` (lines 195–199). Extend
  with a new sub-section AFTER this one (call it
  `### Demonstrated focus and the click-protected viewer`, or fold
  into the existing focus-model section if the next author prefers
  the unified read). The current focus-model copy covers
  `{sidebar, detail}` pane focus only — it does NOT cover the
  in-detail message focus arbitration this topic adds.
- Section: `### Running a search (...)` (line 220). After the
  paragraph on `⌘+G` and `⌘+Shift+G` (line 226), add a short rule:
  Cmd+G, Enter on a result, and clicking a search card ARE
  explicit nav gestures and DO move the viewer. Bubble clicks,
  manual scrolls, and unrelated toggle-driven refetches DO NOT.
- Section: `### Bookmarks (message-level)` (lines 449–455). Brief
  mention: clicking a bookmark also counts as explicit nav and
  moves the viewer to that message, same as a search card click.

**`articles/part_2_web_app_userdoc.md`:**

- Section: `### Overview and the focus model` (lines 153–157). Add
  a parallel short paragraph — keep it to two sentences. Long-form
  story is for the deep-dive twin.

### Suggested framing

Frame this as **the principle of least surprise**, codified. The
user's stated mental model (see global MEMORY.md
[`feedback_least_surprise.md`](.../) — defaults/naming/behavior
should match the expected mental model): when you click a bubble
or scroll manually, you've **demonstrated focus**, and subsequent
state changes (toggle flips, search refetches, auto-promote of
match 0) MUST NOT yank the viewer off your click. The explicit
nav gestures — Cmd+G, Cmd+Shift+G, Enter on a search hit,
clicking a search card, clicking a bookmark — DO move the viewer
because they are explicit signals.

Lead with the user-visible behavior, not the implementation
(`demonstratedFocusUuidRef`, the four-gate auto-promote effect,
the three clear-signals). The implementation is great material
for Part 7 (perf-adjacent) or Part 6 (council debate on
ref-only vs. ref+state-mirror). For Part 2 the takeaway is the
UX commitment, not the React internals.

Subtle but important — this is the kind of polish users feel
without articulating. The article-voice angle: "this is what
'the app respects what you just did' looks like, in detail."

Receipts: commit `0ecdab5` (squashed 4-commit Round-4 cleanup,
two RED-then-GREEN test/fix pairs).

### Screenshots

None required. This is a behavioral guarantee, not a visual
affordance. Optionally: a small animated GIF demonstrating "click
bubble B → toggle Show Tools → viewer stays on B" would be
powerful, but Medium's animated-content support is uneven and
the article doesn't use GIFs elsewhere. Skip.

### User-doc twin sync

Yes — keep it shorter. Two-sentence summary: "When you click a
bubble or scroll manually, the viewer respects that focus.
Search-result clicks, bookmark clicks, and Cmd+G explicitly
move the viewer."

### Cross-refs

- Part 6 (LLM Council): this is a candidate example for the
  council-driven design-debate beat — the commit message
  explicitly cites a "Council 2026-05-26, gpt-5.2" decision
  between ref-only vs. ref + state mirror. Worth adding to
  `PLANS/articles/part6_llm_council_plan.md` as a candidate
  case study (light touch — the bigger council stories already
  in the plan are denser).

---

## Topic 3 — Supervised CC image-cache watcher: detection, banner, install UX

### Article files + sections to edit

**`articles/part_2_web_app.md`:**

- Section: `## Install and First Run` (lines 20–87). The
  `uvx claude-explorer install-watcher` line is ALREADY in the
  happy-path block (line 42) with a strong-recommendation comment.
  Good as-is. But the **consequence** of NOT running it is
  currently invisible from this section — the data-loss framing
  lives only in `### Image caching (Desktop and Claude Code)`
  (lines 413–415). Add ONE sentence in or near line 42's comment
  block: "If you skip this, claude-explorer can only protect
  your screenshots while `serve` is running — Claude Code rotates
  its image cache on its own schedule, and any image rotated
  during downtime is gone." This makes the install-step's
  "Strongly recommended" claim self-justifying without forcing
  the reader to scroll to the caching section.
- Section: `### Image caching (Desktop and Claude Code)` (lines
  413–438). The existing prose covers the install-watcher
  command, the three independent capture paths, and the per-OS
  verification commands. Good. Add a new paragraph (or expand
  the existing `#### Under the hood (for the curious)` block)
  noting:
  - The app **detects whether install-watcher has been run** via
    `/api/health/watcher` (cross-platform — launchd / systemd /
    schtasks probes), and surfaces the result two ways:
  - **A persistent amber banner** at the top of the UI
    (`WatcherMissingBanner`) — preventative, not reactive;
    shows REGARDLESS of whether image-loss events have happened
    yet. Mirrors `ConfigCorruptionBanner` styling.
  - **A startup log line** (4 WARNING lines if missing, 1 INFO
    if installed) so launchd / journalctl tails carry the full
    advice in one place — useful for the supervised-serve case.
  - **The CLI's `serve` command stderr-echoes** the same 4-line
    warning above uvicorn's "Starting server" line, so users who
    launch from the terminal can't miss it.
- Section: `### Image caching (Desktop and Claude Code)` — the
  existing dedup mention (commit `53c2f43` also dedup'd
  permanent-loss WARNING records per-walk via `seen_missing`
  set). Skip mentioning the dedup in the article unless the next
  author wants to write a small "log hygiene" beat; it's
  internal-noise control that doesn't move user behavior.

**`articles/part_2_web_app_userdoc.md`:**

- Section: `### Image caching (Desktop and Claude Code)` (lines
  226–228). Add ONE sentence on the banner and the install
  re-recommendation. Skip the API endpoint, the log lines, the
  CLI stderr echo — all internals for the deep-dive twin.

### Suggested framing

When introducing the watcher, **lead with the data-loss consequence**
(image-cache rotation during downtime), THEN the fix (supervised
launchd / systemd / Task Scheduler job). The current article does
this in the caching section but not in the install section, which
puts the cause and effect ~400 lines apart. The fix: a single
sentence in the install block earns the "Strongly recommended"
label by explaining what skipping it costs you.

For the banner / detection beat in `### Image caching`, frame it
as **defense in depth for a silent failure mode**: the article
already establishes the rotation problem; the new prose adds "and
the app now reminds you, persistently, if you haven't installed
the protection that prevents it." Tie it back to the corrupt-
config banner pattern that already exists in the article (line
579 of the long-form, in the LLM-Council section) — same shape,
different failure mode.

Receipts: commits `53c2f43` (detection + UX + race-fix), companion
plan `PLANS/2026.05.26-watcher-install-detection.md`.

### Screenshots

- **New (required):** the `WatcherMissingBanner` itself, mounted
  at the top of the RootLayout. State to set up: ensure
  install-watcher is NOT installed (uninstall first via
  `uv run claude-explorer install-watcher --uninstall` on a test
  machine — but DO NOT do this on the user's main machine without
  asking), restart `claude-explorer serve`, screenshot the top of
  the UI showing the amber banner + sidebar.
- **Note for capture-session safety:** if uninstalling on the
  user's main machine to capture this screenshot, re-install
  immediately after. Better: capture on a fresh checkout or in
  a separate venv where install-watcher was never run.
- Save as `articles/Attachments/Pasted image 20260526-watcher-missing-banner.png`.
- Filename convention check: the existing files use the format
  `Pasted image YYYYMMDDHHMMSS.png` (no slug). The Cowork
  follow-up plan proposed adding slugs; pick one convention and
  apply consistently. Recommended: `Pasted image
  YYYYMMDD-<slug>.png` to enable readable file listings.

### User-doc twin sync

Yes — the install-section warning sentence belongs in both. The
banner mention belongs in both. The API endpoint / log lines /
CLI stderr echo belong only in the long-form twin's
deep-dive block.

### Cross-refs

- Part 7 (perf postmortem): probably no — this is correctness /
  data-safety, not perf. Skip.
- Part 6 (LLM Council): possibly — the dedup + level-switch was
  a clean "log-hygiene under user observation" pattern (28+
  duplicate log lines in the user's 2026-05-26 incident); could
  be a small example in the council's "log-spam vs silent
  swallow" decision-record beat. Light touch.

---

## Topic 4 — Time Machine restore script: polish, auto-detect, Cowork coverage

### Article files + sections to edit

**`articles/part_2_web_app.md`:**

- Section: `## Your History, On Your Disk` (lines 505–523). The
  current paragraph on `utils/restore-deleted-sessions-and-
  images.sh` (line 519) covers: walks snapshots newest-first,
  restores from `~/.claude/projects/` and `~/.claude/image-cache/`,
  refuses to overwrite existing files, supports `--dry-run`.
  This is now meaningfully out of date. Replace / extend this
  paragraph with:
  - Coverage extended to `~/Library/Application Support/Claude/local-agent-mode-sessions/`
    (Cowork's audit.jsonl tree), in addition to CC sessions and
    image cache.
  - `--tm-disk` is **auto-detected** via `tmutil latestbackup` —
    you don't need to dig up a TM volume UUID anymore.
  - `--continue-on-mount-failure` flag for the case where one
    snapshot mount fails mid-walk (network volume hiccup,
    deleted snapshot) and you want the rest of the walk to
    continue rather than abort.
  - Strict-by-default APFS mount-on-demand: the script only
    mounts the snapshot containing the file you asked for,
    rather than mounting every snapshot upfront. Faster and
    safer.
  - Direct `mount_apfs` invocation (sidesteps `tmutil mount`
    quirks observed on the user's live TM disk).
  - **Fail-fast non-root abort**: the script aborts immediately
    if you forgot `sudo`, saving you a ~30s wait per attempt.
  - APFS orphan-stub filter to skip stubbed-out snapshot entries
    that Time Machine sometimes leaves behind.
- Section: `## Install and First Run` — NO change. The restore
  script lives in `utils/` and is a one-shot recovery tool, not
  part of the happy-path install.

**`articles/part_2_web_app_userdoc.md`:**

- Section: `## Your History, On Your Disk` (lines 268–284). The
  parallel paragraph (line 282) needs the same updates. Keep it
  shorter than the long-form: list the new behaviors (Cowork,
  auto-detect, mount-on-demand, fail-fast) as a bulleted list
  rather than prose if that reads better at user-doc density.

### Suggested framing

Frame this as **the script has graduated from "rough utility"
to "expected to Just Work"**. The user-experience promise:
`sudo ./utils/restore-deleted-sessions-and-images.sh` is a
one-liner — no UUID-digging, no snapshot-by-snapshot babysitting,
no Cowork-shaped gap. The article already has the right hook
("If you're on a Mac and you've already been bitten…" — line
519); just update the capability list to match what shipped.

The fail-fast non-root abort is worth a sentence. It's a tiny
detail but a real UX win — anyone who's typed a long script
invocation, waited 30 seconds, watched it fail, and realized
"oh, sudo" knows the feeling.

Receipts: commit `2b74e31` (extend to Cowork + auto-detect +
APFS mount). The commit's footer notes "an APFS orphan-stub
filter + direct `mount_apfs` invocation are still in-progress on
the working tree" — verify before the article ships that these
landed in a follow-up commit (check `utils/restore-deleted-
sessions-and-images.sh` git log).

### Screenshots

None required. This is a CLI tool; the article should not
screenshot terminal output for a recovery script. (Counter-
argument: a short transcript block showing
`tmutil latestbackup → mount_apfs → restore N files` would
illustrate the auto-detect win nicely. Optional — at the next
author's discretion.)

### User-doc twin sync

Yes — parallel update. Userdoc version is more terse.

### Cross-refs

- Part 7 (perf postmortem): no.
- Part 6 (LLM Council): no.

---

## Topic 5 — Header checkboxes (semantic UI conversion)

### Article files + sections to edit

**`articles/part_2_web_app.md`:**

- Section: `### Tool blocks and slash commands` (lines 165–179).
  The existing prose says "you toggle them on in the conversation
  toolbar" (line 167) and "When the **Tools** toggle is on there's
  an *'Expand / Collapse All Tools'* control" (line 179). Word
  "toggle" still works, but the prose currently implies a button-
  style affordance. Update to refer to a **checkbox** explicitly,
  since the screenshot will show that affordance (see Topic 1's
  screenshot work).
- Section: `### Scope composition` (line 252). References "**Tools**
  toggle in the conversation header" — works fine, no change
  needed (the conceptual model is the same regardless of widget).

**`articles/part_2_web_app_userdoc.md`:**

- Section: `### Tool blocks and slash commands` (line 135). Same
  word-level update: "toggle them on" → "tick the checkbox in
  the conversation toolbar" (or similar).

### Suggested framing

This is small but worth mentioning because the screenshot in
Topic 1 will show the new affordance, and the prose should match.
Frame it as **affordance matches behavior**: the user is
turning a persistent setting on or off (a toggle), not
performing a one-shot action (a button). A checkbox is the
honest widget for that, and it lets a user see at a glance
which toggles are on without having to interpret an icon's
"active" state. No deep dive — one sentence at most.

Don't write a separate section for this; fold the word-level
change into Topic 1's prose update.

Receipts: commit `fe5ec95` (UX fixes including header checkbox
conversion; line in commit msg: "Header toggles (Show Tools,
Show Compactions, etc.) render as semantic checkboxes rather
than ambiguous icon buttons").

### Screenshots

Covered by Topic 1's replacement of `Pasted image 20260515191033.png`.
No additional screenshots needed.

### User-doc twin sync

Yes — same word-level swap. One sentence parity.

### Cross-refs

- None.

---

## Topic 6 — Settings page flash + small-surface UX regressions

### Article files + sections to edit

**`articles/part_2_web_app.md`:**

- Section: `### Settings (`/settings`)` (lines 469–475). The
  current prose covers what the page is and how settings
  persist server-side. NO change required for the
  settings-flash bug — it's an internal regression fix
  (sidebar ConversationList flushSync racing the Settings
  page's mount, causing a flash-and-disappear). User-visible
  symptom was a 2026-05-24 regression that's now gone.

- This bug fits BETTER as a brief sidebar in **Part 7 (perf
  postmortem)** or in the existing **Part 2 LLM-Council
  section** as a "what e2e missed and how the auto-fixture
  caught it" example. The user's MEMORY note on
  `feedback_e2e_console_assertions.md` covers the same lesson.
  The article's existing council section (lines 525–595)
  doesn't yet mention this specific bug; the sidebar in Part 7
  (per the medium-articles.md confirmed-decisions table, line
  82: "the misleading-green e2e — and the harness that catches
  it") is the natural home.

**`articles/part_2_web_app_userdoc.md`:**

- No change.

### Suggested framing

For Part 2 itself: skip. The fix is "what was broken now isn't"
which is changelog territory, not article-voice territory. The
LESSON (DOM-only e2e assertions miss console errors; the auto-
fixture catches them) belongs in Part 7's sidebar OR Part 6's
methodology section, not in the Part 2 product tour.

For wherever this lands (Part 7 sidebar most likely): frame as
"the test that lied" — green checkmark in CI, broken on first
manual test. The user found it by opening dev tools. The
shared `frontend/e2e/fixtures.ts` now asserts zero unexpected
console output per spec; codified as `TESTING.md §5.15`.
This story is already in `PLANS/articles/medium-articles.md` line 82.

Receipts: commit `fe5ec95` (Settings page flash-and-disappear
fix as part of larger UX cluster), commit `3528d22` (e2e
console-error assertions auto-fixture + DYLD bootstrap + §5.15
codification).

### Screenshots

None required.

### User-doc twin sync

No change.

### Cross-refs

- Part 7 (perf postmortem): YES — add to
  `PLANS/articles/part7_perf_postmortem_plan.md` (currently
  empty) as a "misleading-green e2e" sidebar candidate. The
  Settings-flash bug is the canonical example; the auto-fixture
  is the canonical fix.
- Part 6 (LLM Council): possibly — the auto-fixture pattern is
  a council-style "defense in depth" finding. Light touch.

---

## Topic 7 — Unified compact toggle + rich /compact summary rendering

### Article files + sections to edit

**`articles/part_2_web_app.md`:**

- Section: `### Markdown export` (lines 485–493). The current
  prose covers Inline / Bundle CommonMark / Bundle Obsidian and
  the "Save as default" checkbox. After the "one truth, three
  surfaces (viewer, copy, export)" beat on line 491, add a
  parallel sentence (or short paragraph) noting that the
  **Show Compactions** toggle (newly introduced in Topic 1)
  ALSO unifies across viewer / copy / export. The Markdown
  export dialog used to have its OWN per-export `Include /compact
  content` checkbox; that's gone. The dialog now inherits the
  live viewer preference. One source of truth.
- Section: `### Markdown export` — also mention that
  /compact summaries now render as **rich content** (proper
  heading, formatting, embedded content) rather than the raw
  trigger text. This is a visible win in any exported bundle
  containing a /compact boundary.
- Section: `### PDF export (WeasyPrint)` (lines 495–501). Add
  one sentence: the PDF export also picks up the unified
  Show Compactions preference + rich summary rendering, same
  as Markdown.
- Section: `### Settings (`/settings`)` (line 471). The four
  sections list (Appearance / Keyboard Navigation / Data /
  About) doesn't currently call out per-feature toggles like
  Show Compactions or Show Tools (they live in the conversation
  header, not Settings). NO change — but verify when the
  next session reads the actual UI that there isn't a
  Settings-page entry for the compact pref that should be
  mentioned. (Commit `32ee133` mentions
  "export.includeCompactContent pref + Settings toggle"; the
  Settings page may now carry this. Verify by reading the
  actual Settings page or grepping
  `frontend/src/routes/SettingsPage.tsx` for `compact`.)

**`articles/part_2_web_app_userdoc.md`:**

- Section: `### Markdown export` (lines 258–262). Add the
  unified-toggle sentence — keep it terse.
- Section: `### PDF export (WeasyPrint)` (lines 264–266). One
  sentence parity on the unified preference.

### Suggested framing

This is the natural conclusion to Topic 1 (Show Compactions
toggle): the toggle is unified everywhere the user expects it
to be. The current article's "one truth, three surfaces"
framing for the Tools toggle (line 491) is the exact right
template — extend it explicitly to Show Compactions. The
upshot for the user: pick your preference once in the
conversation header, and viewer / copy / Markdown export / PDF
export all honor it without further configuration.

For the rich-summary rendering: frame as "the export captures
what the user sees" — the bundled archive is the conversation
the user was reading, including the rich /compact summary
content (not a stub trigger row). This was a bug-fix that
turns the export from "lossy" to "faithful" for any session
that used /compact.

Receipts: commit `32ee133` (unified compact toggle + rich
/compact summary rendering).

### Screenshots

- **Optional:** a side-by-side of an exported Markdown bundle
  showing BEFORE (stub trigger row) and AFTER (rich summary).
  Probably not worth the effort; a short prose explanation does
  the same work in fewer pixels.
- **None required.**

### User-doc twin sync

Yes — parallel updates in both Markdown and PDF export sections.

### Cross-refs

- None.

---

## Topic 8 — Cowork follow-up items (inherited from superseded plan)

This topic preserves the still-relevant Cowork to-dos from
`PLANS/articles/__archive/2026.05.25-medium-docs-cowork-followup.md`. The Cowork
ingestion code shipped in commit `3edec7b` and the inline doc
weaving shipped in `3d552c9` — but four items remained pending
and are now folded into this larger plan.

### 8.1 — Refresh the source-dropdown screenshot

`Pasted image 20260514121201.png` (line 121 in `part_2_web_app.md`,
line 94 in `part_2_web_app_userdoc.md`) was taken BEFORE Cowork
shipped. The article body now claims four source-dropdown entries;
the image shows three. Replace with a fresh capture showing
`All Conversations | Claude Desktop | Claude Code | Claude Cowork`.

To-do:
1. Start the dev stack (`DYLD_LIBRARY_PATH=/opt/homebrew/lib
   uv run uvicorn backend.main:app --reload --port 8765` +
   `cd frontend && npm run dev`).
2. Open the app, click the source dropdown, screenshot it.
3. Save as `articles/Attachments/Pasted image 20260526-source-
   dropdown-with-cowork.png` (apply the naming convention
   decided in Topic 3).
4. Replace the `<img src="...">` line in both Part 2 files.

### 8.2 — Add the "Show archived" sidebar toggle screenshot

The Cowork-only "Show archived" toggle is mentioned in prose
(line 130 in `part_2_web_app.md`, line 103 in
`part_2_web_app_userdoc.md`) but not screenshotted. Capture a
sidebar shot with `Claude Cowork` selected in the source
dropdown and the toggle visible. Slot adjacent to the prose
mention.

### 8.3 — Verify the user-doc twin caught every Cowork change

Spot-check via:

```bash
diff <(grep -in 'cowork\|local-agent\|archived' articles/part_2_web_app.md) \
     <(grep -in 'cowork\|local-agent\|archived' articles/part_2_web_app_userdoc.md)
```

Expect line numbers to differ; match counts should be roughly
equal. A large delta means the userdoc missed an edit.

### 8.4 — Cowork-restore mention in Time Machine section

The Time Machine restore section (Topic 4 above) MUST mention
Cowork coverage. The script now restores Cowork's
`~/Library/Application Support/Claude/local-agent-mode-sessions/`
tree alongside CC sessions and image cache. This is folded into
Topic 4's edit list.

### 8.5 — Manual UAT against real corpus (STILL PENDING)

The Cowork ingestion code is shipped + tested, but the user has
NOT yet run end-to-end against their real ~42-Cowork-session
corpus. UAT checklist (verbatim from the superseded plan):

1. `sudo ./utils/restore-deleted-sessions-and-images.sh --dry-run` →
   confirm Cowork files appear in the dry-run plan.
2. `sudo ./utils/restore-deleted-sessions-and-images.sh` → actually
   restore.
3. Restart `claude-explorer serve`. Watch lifespan log for "Loaded
   N Cowork sessions from ~/Library/Application Support/Claude/...".
4. Sidebar → select "Claude Cowork" → expect ~42 sessions with
   sensible titles.
5. Search "Marc" → expect at least one Cowork hit; click →
   conversation pane opens with messages.
6. Export one Cowork session to Markdown → content survives, no
   `_audit_hmac` leak.
7. Open Claude Desktop, archive one Cowork session, refresh
   Explorer → it disappears. Toggle "Show archived" → it
   reappears.
8. Confirm the error-field banner renders if any Cowork session
   has a non-null `error`.

This UAT MUST complete before the article ships to Medium — the
article should reflect verified behavior, not unverified design
intent.

---

## Topic 9 — Truncation indicator (999+ on the match counter)

### Article files + sections to edit

**`articles/part_2_web_app.md`:**

- Section: `### Running a search (...)` (line 226). The current
  prose says: *"the search panel header carries a small inline
  'N of M matches' counter so you can see your position at a
  glance."* Extend with a sentence on truncation: when the
  result set exceeds the HTTP per-request limit (currently
  1,000 messages — see the existing truncation-disclosure
  paragraph on line 329 for the implementation detail), the
  counter shows a `999+` (or similar) indicator and the panel
  footer suggests refining the query. This is the at-a-glance
  signal that there's MORE to find.
- Verify whether the indicator is `999+`, `1000+`, or
  `>1000` — the existing truncation paragraph (line 329)
  describes the wire format
  (`total_messages_matched`, `returned_messages`, `truncated`)
  but the UI's exact glyph may differ. Grep
  `frontend/src/components/search/` for the implementation
  before drafting prose.

**`articles/part_2_web_app_userdoc.md`:**

- Section: `### Running a search (...)` (line 179). Add one
  short sentence on the truncation indicator. Keep it user-
  facing: "if you see 999+ in the counter, you're hitting the
  per-query cap — refine the query to see narrower results."

### Suggested framing

Frame as **honesty about limits**. The corpus can produce
thousands of matches for a broad query; the wire format caps
the response at 1,000 messages to keep the panel fast. The
counter showing 999+ tells the user three things at once:
(a) there are more hits than the panel is showing, (b) the
top 1,000 (BM25-ranked) are what's visible, and (c) refining
the query will surface the rest. This is the user-friendly
expression of the deeper truncation-disclosure work already
documented in the long-form (line 329).

Receipts: this work shipped earlier (the truncation paragraph
on line 329 covers the wire-format side from a prior commit
batch), but the at-a-glance UI signal is recent enough to
warrant a sentence here. Verify the commit that added the
UI counter affordance — likely part of the search-perf or
search-focus cluster (`51f9def`, `74c4d6f`, or earlier).

### Screenshots

- **Optional:** a small inset of the SearchPanel header showing
  `999+ matches` for a broad query. Probably worth doing as
  part of the larger SearchPanel screenshot refresh (Topic 1's
  header capture).
- Not blocking — prose can stand on its own.

### User-doc twin sync

Yes — one sentence parity.

### Cross-refs

- None.

---

## Topic 10 — Performance hunt + virtualization (Part 7 territory, with Part 2 cross-refs)

### Article files + sections to edit

**`articles/part_2_web_app.md`:**

The bulk of the performance hunt + virtualization narrative
belongs in **Part 7 (perf postmortem)**, not Part 2. Part 2's
existing performance subsection (lines 274–383) already covers
the FTS5 + persistent summary cache + drift-watcher + payload-
trim + sidebar-virtualization story at depth — that subsection
is the "Part 7 trailer" baked into Part 2 from earlier rounds
of the article.

That said, several recent perf wins MAY warrant tactical
updates to Part 2's existing perf table or paragraphs:

- Section: `### Performance (FTS5 index)` table (lines 286–301).
  Verify whether any of the headline numbers in this table
  need updates from the more recent perf work (commit
  `51f9def` notes "~95% warm-switch reduction", "first cold
  search reduced ~5s via lifespan warmup", "Drop async from
  /api/search route handlers", "gzip-middleware bypass for
  /api/conversations/<uuid> — burning ~700ms per warm hit").
  Some of these may already be captured in the table; others
  (especially the bubble-list virtualization win for warm
  conversation switch) likely are NOT. Cross-check by
  reading the table carefully and the commit message
  in full.
- Section: `### Performance (FTS5 index)` (line 326 onward,
  schema bumps v6 → v7 → v8 → v9). Verify whether the v10 /
  v11 / v12 / v13 / v14 schema bumps from commits `51f9def`
  and `7832b86` need a brief mention in this section, or
  whether they belong entirely to Part 7. Suggested: brief
  mention in Part 2 ("the schema has continued to evolve;
  see Part 7 for the full migration history") with the
  detailed walk reserved for Part 7.
- DO NOT rewrite the existing performance subsection. Part 2
  already has substantial perf depth; the goal here is
  incremental updates, not a rewrite.

**`articles/part_2_web_app_userdoc.md`:**

No change required for performance. The userdoc twin is
deliberately lighter on perf depth.

### Part 7 work (the bulk of this topic)

`PLANS/articles/part7_perf_postmortem_plan.md` does not yet
exist (per `PLANS/articles/medium-articles.md` confirmed-decisions row
on Part 7, the plan is "TODO at next planning session"). The
following are the additional inputs Part 7 should incorporate
when its plan is written:

1. **The five-belief falsification chain** documented in
   `PLANS/POSTMORTEM-search-typing-lag-2026-05-22.md`. This
   IS the centerpiece of Part 7's first act per the existing
   confirmed-decisions row (line 80 of medium-articles.md).
2. **The lifespan warmups (W1–W5)** — FileCache pre-warm,
   FTS5 warmup queries, title-sweep projection table for 24×
   cold speedup on title-only queries. Commit `51f9def`.
3. **The gzip-event-loop trap** — `GZipMiddleware` blocking
   the event loop on 1–5MB JSON bodies for ~700ms. The fix
   was bypass via `SelectiveGZipMiddleware` per route +
   `ORJSONResponse` + cached `model_dump` dict. Commit
   `51f9def`. This story is already cited in Part 2 line 80
   of medium-articles.md.
4. **Frontend virtualization of the bubble list** — Part 2
   already covers sidebar virtualization (lines 377–381);
   Part 7's third act should cover the bubble-list
   virtualization that took warm conversation switch from
   10.3s to 514ms (95% reduction). Commit `51f9def`.
5. **The Cowork-search recovery (v11 → v12 migration)** — a
   bug where the schema migration orphaned Cowork rows in
   `indexed_files` because `SearchIndex.delete_by_path` was
   deriving conv_uuid via `file_path.stem`, which works for
   CC and Desktop but yields literal `audit` for Cowork.
   The recovery migration is a nice "the migration that
   knew it was wrong and fixed itself" story for Part 7's
   third act. Commit `51f9def`.
6. **The Show Compactions self-healing schema migration**
   (Topic 1's v13 → v14 race-fix). Same shape. Commit
   `7832b86`.
7. **The schema-version-stamp-vs-actual-shape mismatch
   handling** — `_init_schema` now verifies both messages
   AND conversations columns match the expected v14 shape
   before trusting the schema_version stamp. If they don't,
   self-repairs (ALTER + backfill). This is the "trust the
   reality, not the version stamp" pattern. Commit
   `7832b86`.
8. **The async-handler-blocking-the-event-loop discovery** —
   `/api/search` route handlers were `async def` but doing
   CPU-bound SQLite work synchronously, blocking the event
   loop for the entire query duration. Fix: drop the `async`
   so FastAPI thread-pools the work; explicit client-
   disconnect bail. Commit `51f9def`. This is the canonical
   "async ≠ async" trap and earns a paragraph.

### Suggested framing for Part 7

Part 7 already has its frame established in
`PLANS/articles/medium-articles.md` (three acts: search-typing storm,
backend, render). The 2026-05-25 perf work is the third
"sequence of wins" pass that arrived after that outline was
written. Frame the new wins as the **second wave** of the
hunt — same methodology (measure, baseline, hypothesize,
falsify), applied to surfaces the first wave didn't touch
(lifespan warmups, gzip middleware, bubble-list virtualization,
schema migration self-healing).

The Cowork-search-recovery + self-healing schema story is
worth a section to itself because it demonstrates the
methodology applied to a regression caused BY the perf work
(not a separate problem) — a nice meta beat.

### Screenshots

None for Part 2 updates. Part 7 will have its own screenshot
needs; out of scope for this plan.

### User-doc twin sync

No change.

### Cross-refs

- Part 2 → Part 7: add explicit cross-link in Part 2's
  `### Performance (FTS5 index)` subsection ("the full
  story — including the search-typing-storm postmortem,
  the bubble-list virtualization, and the schema-migration
  self-healing — lives in Part 7"). One sentence.
- Part 7 → Part 2: bidirectional; Part 7 should cross-link
  back to Part 2's existing performance-table for the
  baseline numbers.

---

## Confirmed-decisions table updates

Each topic above that lands as a sustained doc change earns a
row in the `PLANS/articles/medium-articles.md` Confirmed Decisions table.
Proposed rows (next session inserts these via Edit into the
existing table after the Part 6 / Part 7 rows):

| Decision | Choice |
|---|---|
| Show Compactions toggle in Part 2 | Documented as a filter (not a destructive operation), with multi-source detection (CC isCompactSummary, CC text-prefix, Cowork text-prefix) and title-leak filter. Schema-bump narrative reserved for Part 7. |
| Demonstrated-focus arbitration in Part 2 | Documented as a UX commitment: clicks and manual scrolls protect viewer position; Cmd+G / Enter / card click / bookmark click are explicit nav. Implementation reserved for Part 6 / Part 7. |
| Supervised CC image-cache watcher detection in Part 2 | Install-step warning sentence + caching-section banner / endpoint / log-line mention. Cross-OS detection via launchd / systemd / schtasks probes. |
| Time Machine restore script polish in Part 2 | Cowork coverage + `tmutil latestbackup` auto-detect + APFS mount-on-demand + `--continue-on-mount-failure` + fail-fast non-root. |
| Header-checkbox UX in Part 2 | Word-level: "toggle" prose replaced with "checkbox" prose where the conversation-header toggles are referenced; screenshot refresh shows the new affordance. |
| Unified compact toggle + rich /compact rendering | One source of truth: viewer + copy + Markdown + PDF all honor the same Show Compactions preference; rich-summary rendering replaces the stub trigger row in exports. |
| Truncation indicator (999+) in Part 2 | One-sentence user-facing note in the search section; full wire-format / capacity story stays in the existing truncation-disclosure subsection. |
| Settings-page flash sidebar | Sidebar in Part 7 (the misleading-green e2e), not in Part 2 prose. Codified rule `TESTING.md §5.15`. |
| Part 7 plan trigger | The 2026-05-25 perf hunt (commit `51f9def`) plus the schema self-healing (commit `7832b86`) constitute the second wave the Part 7 plan must capture; Part 7 plan TODO escalates. |

---

## Verification checklist

After the article edits land, the next session should run:

```bash
# 1. Spot-check long-form + userdoc parity for the new topics
diff <(grep -inE 'compact|cowork|watcher|checkbox|demonstrated|truncat|999' articles/part_2_web_app.md) \
     <(grep -inE 'compact|cowork|watcher|checkbox|demonstrated|truncat|999' articles/part_2_web_app_userdoc.md)

# 2. Verify no stale references to button-style Tools toggle remain
grep -inE 'tools.{0,20}button|button.{0,20}tools' articles/part_2_web_app.md articles/part_2_web_app_userdoc.md

# 3. Verify the source-dropdown screenshot is the post-Cowork capture
ls -la "articles/Attachments/Pasted image 20260514121201.png"  # if this still exists and is referenced, the swap didn't happen

# 4. Verify the watcher-banner screenshot exists
ls -la "articles/Attachments/Pasted image 20260526-watcher-missing-banner.png"

# 5. Spot-check Confirmed Decisions table updates landed
grep -inE 'Show Compactions|Demonstrated-focus|Supervised .* watcher|Time Machine restore|Header-checkbox|Unified compact|Truncation indicator' PLANS/articles/medium-articles.md
```

Additionally, the Cowork UAT (Topic 8.5) MUST complete before
the article ships to Medium. The article is a credibility
artifact; it must reflect verified behavior, not unverified
design intent.

---

## Out of scope for this plan

The following changes from the `3d552c9..HEAD` commit range are
internal-only and do not warrant Medium article coverage:

- **Commit `a4ac15d` (`test(cc): pin resume-session dedup
  contract`)** — internal test pinning a regression contract.
  The dedup behavior itself was already documented; this test
  prevents regression. No user-visible surface change.
- **Commit `593f661` (`refactor: code-review sweep`)** — file
  split refactor (`backend/export.py` → `backend/exporters/`,
  `backend/search.py` → `backend/search_text.py` extraction,
  `fetcher/cli.py` → `cli/main.py` rename, frontend message
  blocks subtree). No user-visible behavior change. WORTH
  mentioning in Part 5 (build story 2) or Part 6 (LLM
  Council) as a council-driven hygiene pass, but NOT in
  Part 2.
- **Commit `3528d22` partial — DYLD bootstrap** — the
  `backend/main.py` import-time DYLD path bootstrap is a dev-
  environment quality-of-life fix; the article's existing
  install instructions already cover the user-facing
  `DYLD_LIBRARY_PATH=/opt/homebrew/lib` invocation for the dev
  server. The bootstrap fix means users on macOS Sonoma+ who
  forget the env var will still get WeasyPrint working — worth
  a one-sentence note in `## PDF Export Dependencies` in
  CLAUDE.md (already there) but NOT in the Medium article.
- **Commit `930f47f` (`docs(plans,perf)`)** — pure
  documentation / plan artifacts. By definition not user-
  visible; meta-content for future articles to mine.
- **Commit `7832b86` partial — autouse test fixture
  `_block_real_search_index_path`** — a test-isolation
  improvement. Not user-visible.
