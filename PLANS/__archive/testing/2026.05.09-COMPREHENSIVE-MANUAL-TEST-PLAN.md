# Comprehensive manual testing plan — 2026-05-09 / 2026-05-10 (V1-readiness)

**This is the single plan to walk for V1 release.** It supersedes the
per-day plans (`2026.05.06-`, `2026.05.07-`, `2026.05.08-`,
`2026.05.09-MANUAL-TEST-PLAN.md`), which remain in `PLANS/` as
historical record of what landed each day.

**Last updated 2026-05-10** to fold in: Cmd+F search-tab fix, image
failure tombstone (10x), auto-warm CC cache at startup, install-watcher
launchd installer, watcher poll interval 60s→5s.

**Order:** every section is ordered by V1 risk. Walk top-to-bottom; if
you run out of time, the bottom sections are the safest to skip.

**Time budget:** ~50-65 min for full walk. The "RAPID" budget below
hits the load-bearing surfaces in ~25 min.

**RAPID path (~25 min) if you need to bail early:**
- §1 (Setup, 1 min)
- §2 (Automated baseline, 3 min)
- §3 (Broken images + tombstone, 4 min)
- §4 (Connection dialog UX, 3 min)
- §5 (Search auto-focus, 2 min)
- §5b (Cmd+F always opens Search tab, 1 min)
- §6 (Composable filter picker, 4 min)
- §10 (Filter trash + delete UX, 3 min)

Skip everything else if RAPID. Otherwise, continue through §23.

---

## §1 — Setup (1 min)

```bash
cd /Users/rpeck/Source/claude-desktop-message-exporter
DYLD_LIBRARY_PATH=/opt/homebrew/lib uv run uvicorn backend.main:app --reload --port 8000 &
cd frontend && npm run dev
```

Open http://localhost:5173.

**Pass:** sidebar shows your real conversations; no "Connecting to
Backend" modal cycles; no "Last error: …" toast at any point during
cold-start. **(If you see EITHER of those on the initial load, that's
a regression of the V1 connection-dialog fix — capture it and tell
me.)**

### §1.1 — One-time setup: install the always-on CC watcher (recommended)

Without this, the CC image watcher only runs while
`claude-explorer serve` is up. Quitting the dev server lets Claude Code
rotate images off disk before our cache catches them — permanent data
loss.

```bash
uv run claude-explorer install-watcher       # writes a launchd plist; runs at login
launchctl list | grep claude-explorer        # verify it's loaded
```

**Pass:** `launchctl list` shows `com.claude-explorer.cc-watcher` with
PID > 0 and exit code 0. Logs at
`~/Library/Logs/claude-explorer-cc-watcher.{out,err}`.

Skip if you've already done this once. Uninstall with
`uv run claude-explorer install-watcher --uninstall`.

### §1.2 — Auto-warm runs at backend startup (no CLI required)

The backend's lifespan now spawns a background warm pass that walks
every CC session JSONL on disk and copies referenced
`[Image: source: ...]` files into the permanent cache. Idempotent —
re-runs on every restart, costs nothing if already done.

You don't have to do anything. Confirm by tailing the uvicorn log
within ~30s of starting the server:

```
INFO ... CC warm pass complete: N session(s) walked, N with markers,
         N files cached, 0 failed.
```

**Pass:** the line appears with `failed: 0`. If files cached >0 you
just protected images that would have been at risk on the next CC
rotation — exactly what we want.

---

## §2 — Automated baseline (3 min)

If both pass, the rest of the manual walk is verifying user-visible
behavior. If either fails, stop and fix before continuing.

### §2.1 — Backend pytest sweep

```bash
cd /Users/rpeck/Source/claude-desktop-message-exporter
uv run pytest backend/tests -q
```

**Pass:** `290 passed, 0 skipped, 0 failed`.

(Test count grew from 225 → 274 with the 2026-05-10 SQLite FTS5
search-index work: 31 unit tests for the index itself
(`test_search_index.py`), 13 equivalence tests pinning byte-for-byte
parity between FTS5 fast path and linear-scan fallback
(`test_search_equivalence.py`), 3 watcher-integration tests
(`test_cc_image_watcher.py`), and 2 local-only perf benchmark tests
(`test_search_index_benchmark.py` — auto-skipped on CI; set
`RUN_SEARCH_BENCHMARK=1` to force-run locally). 274 → 290 with the
2026-05-11 `include_tool_calls` server-side filter: 16 tests in
`test_search_include_tool_calls.py` pinning the new contract that
search ignores tool/thinking content when the UI's "Show tool calls"
toggle is off, with bidirectional inversion checks and FTS5/linear
equivalence under the filter.)

(No `--ignore` flags or DYLD env-var prefix needed — conftest
bootstraps it. If you see "WeasyPrint" SKIPs, run `brew install
pango cairo libffi`.)

### §2.2 — Playwright deterministic check (skip if you trust CI)

```bash
cd frontend
./node_modules/.bin/playwright test --reporter=line --workers=4 --retries=0 --repeat-each=2
```

**Pass:** `620 passed`. ~3.5 min.

---

# Part A — V1-polish changes (today, 2026-05-09)

## §3 — Broken images: which fix applies to which class (4 min)

There are TWO distinct broken-image classes. The fixes that landed
this sweep cover them differently. Read this table before walking
the sub-sections — picking the wrong one wastes time.

| Class | Lives at | Fix that helps | Recoverable now? |
|---|---|---|---|
| **Desktop attachment** (claude.ai-served, on `CLAUDE_AI` conversations) | `~/.claude-explorer/files/<conv>/<file>/preview.*` (we cache at fetch) | `_proxy()` local-cache fallback (`eb112f8`, May 9) | **YES** if the local cache file exists — the fix serves it on upstream 404 |
| **CC marker** (`[Image: source: ~/.claude/image-cache/<sess>/N.png]` in `CLAUDE_CODE` sessions) | `~/.claude/image-cache/<sess>/N.png` (CC writes here, then rotates) | Watcher + auto-warm + launchd installer (May 6 onward) | **NO** if CC already rotated it before any of our protection landed. The watcher only protects images created AFTER it was running. |

If a specific CC image is showing "Image not in cache: <N>.png" right
now, **that image is unrecoverable** — CC already rotated the bytes;
there is no copy on disk anywhere. The launchd watcher + auto-warm
prevent the same fate for FUTURE CC images.

### §3.1 — Desktop attachment fallback (`_proxy()` fix)

Targets the first row in the table above. To verify:

1. Pick a CLAUDE_AI conversation with images you know fetched recently.
   Confirm they render (regression baseline).
2. Look at `~/.claude-explorer/files/` — pick a
   `<conv>/<file>/preview.*` that exists on disk. Open that
   conversation; the image renders (could be from upstream OR from
   local cache — either is correct).
3. **Backend log signal (optional):** `tail -f` the uvicorn output as
   you scroll through old conversations; you should see a
   `proxy_local_fallback` log line per cache-served image and zero
   per upstream-served image.
4. **Exports also fixed:** export a CLAUDE_AI conversation that
   contains a previously-broken Desktop image as Markdown (header →
   Markdown → Inline). The image is present in the rendered output.
   Same for PDF.

**Pass:** every CLAUDE_AI conversation's images render OR show
the friendly fallback tile (no broken-image icon).

### §3.2 — CC marker protection (watcher + auto-warm)

Targets the second row in the table. CC images you've seen broken
with "Image not in cache: <N>.png" on existing conversations stay
broken — those bytes are unrecoverable. This step verifies the
protection for NEW CC images going forward.

1. Verify the launchd watcher is running:
   ```bash
   launchctl list | grep claude-explorer
   ```
   **Pass:** PID > 0, exit code 0.
2. Use Claude Code in any session. The next time you paste an image
   (CC writes a new `<N>.png` to its image-cache), within 5 seconds
   you should see a copy at
   `~/.claude-explorer/cc-images/<sess>/<sess>--<N>.<sha>.png`.
3. Open that conversation in the explorer; image renders.

**Pass:** new CC images appear in the permanent cache within 5s of
CC writing them, regardless of whether the dev backend is up.

### §3.3 — Image-failure tombstone (no infinite re-fetches)

For images in EITHER class that are genuinely gone (no local cache),
the frontend now tombstones the URL after 10 failed fetches per
session. Subsequent renders skip the `<img>` and render the fallback
tile directly — no network requests.

To verify (optional, requires DevTools):

1. Find a conversation that has at least one truly-broken image (the
   "Image not in cache: <filename>" fallback). A rotated CC image
   from §3 row 2 is a perfect candidate.
2. Open DevTools → Network tab, filter by "preview" or "thumbnail" or
   "cc-image". Clear the network log.
3. Scroll the broken image OUT of the viewport, then back IN. Repeat
   ~5 times.
4. Initial: each round trip fires 1-2 requests (cache-buster retry).
   After ~5 mounts (10 failures total), no further requests fire.
5. **Pass:** Network panel shows the request count plateaus around
   the 5th remount. The fallback tile keeps rendering.

Reload the page to clear the tombstone (failures often transient —
restart gives every URL a fresh chance).

---

## §4 — Connection dialog UX (3 min)

Three bundled sub-fixes. Reproduce by stopping the backend.

```bash
# In the terminal running uvicorn, hit Ctrl+C.
```

Now reload the browser tab. Watch the dialog carefully:

1. **No instant flash:** the dialog should NOT appear immediately. A
   brief connection failure (≤4s) does not surface UI at all.
2. **Spinner-only at retry 2 (~5–10s in):** the dialog opens with
   "Connecting to Backend", spinning icon, "Attempt 2 of 5". You
   should NOT see a red "Last error: Failed to fetch" line. **That
   was the UGH.**
3. **Error appears after terminal failure (~42s):** dialog now says
   "Connection Failed" and the red "Last error: …" line appears
   underneath. (Correct — we want the error visible once we've
   stopped trying.)

Restart the backend:

```bash
DYLD_LIBRARY_PATH=/opt/homebrew/lib uv run uvicorn backend.main:app --reload --port 8000 &
```

Click **Try Again** in the dialog. Briefly shows "Connecting", then
closes when backend responds.

**Pass:** all 3 behaviors. The UGH is gone.

---

## §5 — Search auto-focus first match (2 min)

When search results land, the conversation panel auto-scrolls to the
first match and applies a yellow ring (no Cmd+G needed).

1. Open any conversation. Press Cmd+K to open the search panel.
2. Type a query that has matches in this conversation. Wait for the
   result list to populate.
3. **Without clicking anything**, look at the conversation panel: the
   first matching message should be in view AND have a yellow ring.
   Ring fades after ~2s.
4. Press Cmd+G. Conversation panel scrolls to match 2 (yellow ring).
5. Type a different query. Panel auto-scrolls to that query's first
   match.

**Edge case (don't yank back):**

6. Type a query with 2+ matches. Wait for auto-focus on match 1.
7. Press Cmd+G to advance to match 2.
8. **Wait 3 seconds without doing anything.** The panel must NOT snap
   back to match 1.

**Pass:** all 5 plus the don't-yank check.

---

## §5b — Cmd+F always opens the Search tab (1 min)

Bug fixed 2026-05-09: `rightPaneTab` is persisted in server-side
preferences. If you ever clicked the Bookmarks tab, every subsequent
Cmd+F would open the right pane on Bookmarks — not what "find" muscle
memory expects.

1. Click the **Bookmarks** tab in the right pane (or activate any
   bookmark to land you there).
2. Close the right pane (Cmd+K, or close button).
3. Open a conversation.
4. Press **Cmd+F**.
5. **Pass:** right pane opens with **Search** tab active (NOT
   Bookmarks). The search input is focused, ready for typing.

Reload the page and verify Cmd+F still opens Search tab even though
the persisted preference says Bookmarks.

**Pass:** Cmd+F always lands on Search regardless of last-selected tab.

---

# Part B — Composable filters (2026-05-07 — never fully tested)

The user reported finding many bugs in 05.07's manual walk and didn't
finish. Walk the entirety of Part B carefully.

## §6 — Active filter picker (sidebar, 4 min)

Between the title-search input and the source-filter dropdown there's
a `Filter:` Select.

1. Picker should default to **All conversations**.
2. Click the picker. Dropdown lists:
   - **All conversations** (sentinel)
   - separator
   - Every enabled saved filter, alphabetically (atoms + groups)
   - separator
   - **Manage filters…** at the bottom with a small gear icon
3. Click **Manage filters…**. Modal opens. Close with Esc or X.
   - **Pass:** picker's displayed value is unchanged. Picking
     "Manage filters…" must NOT mutate the active filter.
4. Pick any saved filter. Sidebar narrows accordingly.
5. Pick **All conversations**. Sidebar shows everything.
6. Reload the page. Selection persists (server-side preferences).

---

## §7 — Manage Filters: atom editor (5 min)

1. Open Manage Filters (from picker dropdown).
2. **+ New filter**. Right pane shows empty atom editor with:
   - **Name** input (placeholder: *Name (auto-filled from first pattern)*)
   - **Type**: Atom (selected) | Group
   - **Enabled** toggle (on)
   - **Behavior**: ⦿ Hide matches | ○ Show only matches
   - **Mode**: ⦿ Glob | ○ Regex
   - **Patterns** textarea
   - Plain-English **summary line**

### §7.1 — Name auto-fill rules

3. Type `Scan Gmail*` into Patterns. Wait ~300ms.
   - **Pass:** Name fills with `Scan Gmail` (glob meta-chars stripped).
4. Add a second line `cron job` to Patterns.
   - **Pass:** Name doesn't change (auto-filled from line 1; line 2 is
     fallback only).
5. Type into Name (e.g. add `(work)`).
   - **Pass:** auto-fill stops. Modify Patterns; Name doesn't change.
6. Clear Name back to empty.
   - **Pass:** auto-fill resumes from current Patterns.

### §7.2 — Behavior radio updates summary in real time

7. Patterns: `cron1` and `cron2`. Behavior: **Hide matches**.
   - **Pass:** summary reads *"Hides conversations whose titles match
     any of: cron1, cron2."*
8. Click **Show only matches**.
   - **Pass:** summary updates immediately (no Save needed) to *"Shows
     only conversations whose titles match any of: cron1, cron2."*

### §7.3 — Mode radio re-evaluates active filter in-session

9. Save the filter. Activate from picker. Note visible conversations.
10. Re-open the filter, switch Mode from Glob to Regex (tweak patterns
    to be regex-valid if needed). Save.
    - **Pass:** sidebar updates without page reload.

### §7.4 — "Used by:" line

11. Create a second atom called `Foo` with patterns `*foo*`. Save.
12. Create a Group containing both atoms (see §8). Save with name
    `Combined`.
13. Open the `Foo` atom in the editor.
    - **Pass:** under the Name input there's a *"Used by: Combined"* line.
14. Open `Combined`. Remove `Foo`. Save.
15. Re-open `Foo`.
    - **Pass:** the Used-by line is gone.

### §7.5 — Enabled toggle syncs both directions

16. The atom row in the left list has a checkbox. The editor's Enabled
    toggle is the second one.
17. Click the row checkbox. Editor toggle flips immediately.
18. Click the editor toggle. Row checkbox flips immediately.

---

## §8 — Manage Filters: group editor (4 min)

1. Click **+ New filter** → switch **Type: Group**. Editor shape changes:
   - Name input
   - Enabled toggle
   - **Match**: ⦿ all of these | ○ any of these (no AND/OR jargon)
   - **Members** chips list (initially empty)
   - **Add member** Select + Add button
   - Plain-English summary line

2. Click **Add member**.
   - **Pass:** every other enabled filter appears as an option.
   - **Pass:** the group itself does NOT appear (no self-reference).
   - **Pass:** any filter that already references this group
     transitively does NOT appear (cycle prevention).

3. Add two atoms as members. Each appears as a chip with `×`.
   - Click `×` on a chip → member removed.

4. Disable one of the member atoms (toggle in left list, refocus group).
   - **Pass:** the disabled atom in **Add member** Select shows with
     `(disabled)` suffix.

5. Save. Activate the group. Sidebar narrows according to the Match
   logic over the members' Behavior decisions.

---

## §9 — Canonical composition examples (3 min)

### §9.1 — One atom hides multiple patterns (the cron case)

1. New atom. Behavior: **Hide matches**. Mode: Glob.
2. Patterns:
   ```
   cron1
   cron2
   ```
3. Save. Activate.
4. **Pass:** every conversation whose title matches `cron1` OR `cron2`
   (glob substring match) is hidden.

### §9.2 — Two hide-atoms ANDed via group

1. Atom A: Behavior=Hide, Patterns=`cron1`.
2. Atom B: Behavior=Hide, Patterns=`cron2`.
3. Group G: Match=`all of these`, Members=[A, B]. Activate.
4. **Pass:** rows where neither cron1 nor cron2 matches are visible;
   rows that match either pattern are hidden.

The atom shortcut (§9.1) is simpler; the group form is necessary only
if you want to enable/disable A or B independently.

---

## §10 — Filter trash + delete UX (3 min)

1. Open Manage Filters. Each row contains:
   - Name
   - Type tag (small uppercase grey text)
   - Enabled checkbox
   - **Trash icon** (red on hover) at the far right edge

2. **Pass:** the trash icon is fully visible on every row, NOT clipped
   by the right edge of the left pane.
3. Hover trash → turns red. Tooltip: *"Delete <filter name>"*.

### §10.1 — Delete unreferenced filter

4. Click trash on an unreferenced filter (e.g., `Foo` after removing
   it from any group).
5. Inline below the row: *"Delete <name>?"* with **Cancel** and
   **Confirm**.
6. **Cancel** → prompt collapses, filter remains.
7. Trash → **Confirm** → filter removed.

### §10.2 — Delete referenced filter is blocked

8. Click trash on an atom that's a group member.
9. Inline message: *"Can't delete: used by <group name>. Remove it
   from those groups first."*
10. Filter is NOT deleted. Click `Dismiss` to close.

### §10.3 — Deleting the active filter clears the active selection

11. Activate any filter from the picker.
12. Open Manage Filters, delete that filter.
13. Close modal.
14. **Pass:** picker shows **All conversations** (active was cleared).

---

## §11 — Migration: legacy state → v2 graph (3 min)

Live preferences should already have `_migratedV1: true` and
`_migratedV2: true`.

1. ```bash
   cat ~/.claude-explorer/preferences.json | python3 -m json.tool | grep -E "_migratedV[12]|polarity|savedFilters|activeFilterIds"
   ```
   - **Pass:** `_migratedV1: true` AND `_migratedV2: true` present.
   - **Pass:** no `polarity` key remains anywhere (atoms use `behavior`
     now).
   - **Pass:** `savedFilters` and `activeFilterIds` are absent or null
     (legacy tombstones).

### §11.1 — Migration banner

2. To force a re-test:
   ```bash
   cp ~/.claude-explorer/preferences.json /tmp/prefs.bak.json
   python3 -c "import json,pathlib; p=pathlib.Path.home()/'.claude-explorer/preferences.json'; d=json.loads(p.read_text()); d['data']['filters'].pop('migrationBannerDismissed', None); p.write_text(json.dumps(d, indent=2))"
   ```
3. Reload.
4. **Pass:** amber banner above conversation list: *"Filters are now
   composable. Your previously-pinned filters are grouped under
   **Default (migrated)** — your active filter. Click *Manage
   filters* to review."*
5. Click dismiss (X or *Dismiss*).
6. **Pass:** banner disappears. Reload — banner stays gone.

---

## §12 — Cycle defense (2 min)

1. Create atom A.
2. Create group G containing A.
3. Create group G2 containing G.
4. Open G in editor → **Add member**. Select must NOT list G2.

### §12.1 — Manually-edited cycle

5. With backup taken (§11.1), edit `~/.claude-explorer/preferences.json`
   to introduce a cycle (G2.childIds → G AND G.childIds → G2).
6. Reload.
7. **Pass:** app loads without crashing. Sidebar still works. Active
   filter (if pointing at the cyclic group) becomes a no-op.
8. Restore: `cp /tmp/prefs.bak.json ~/.claude-explorer/preferences.json`
   and reload.

---

# Part C — Backend security + integrity smoke (2026-05-08)

Most of 05.08 is auto-verified by pytest. Only the live curl smoke
is worth doing manually for V1 confidence.

## §13 — Attachments path-traversal rejection (2 min)

Backend must return 4xx with no body leak. The plan baseline allows
either 400 (the new check) OR 404 (resolved-target doesn't exist) — the
key is "no on-disk bytes in the response body".

```bash
# Backend should be running on localhost:8000.

curl -sS -i "http://localhost:8000/api/attachments/..%2F..%2Fetc/file/original" | head -2
curl -sS -i "http://localhost:8000/api/attachments/conv1/..%2F..%2Fetc/original" | head -2
curl -sS -i "http://localhost:8000/api/attachments/conv1/file1/notreal" | head -2
```

**Pass:**
- First two: 400 or 404. Body is JSON `{"detail": ...}`. **NEVER** a
  200 with on-disk file bytes.
- Third: 400 (invalid variant).

## §14 — Atomic-write `.tmp` cleanup (1 min)

```bash
ls -la ~/.claude-explorer/*.tmp* 2>/dev/null
```

**Pass:** no output (or `No such file or directory`). If you have any
`preferences.json.tmp` or `bookmarks.json.tmp` from a previous failed
write, delete them as orphans:

```bash
rm -f ~/.claude-explorer/preferences.json.tmp ~/.claude-explorer/bookmarks.json.tmp
```

## §15 — SSE wire format reality check (OPTIONAL, 1 min)

```bash
curl -N -sS "http://localhost:8000/api/fetch/refresh?incremental=true" | head -20
```

**Pass:** every emitted line is either:
- `data: {"type": "...", ...}` — JSON event
- `: ping` — SSE-comment keep-alive (capture phase only)
- blank line (frame separator)

There should be NO `event: <name>` lines.

---

# Part D — Older-surface carry-over (2026-05-06)

These flows didn't change today; quick smoke to confirm no regression.
Skip Part D entirely if you're short on time AND nothing in Parts
A–C surfaced anomalies.

## §16 — Server-side preferences cross-browser (3 min)

### §16.1 — Single-browser persistence

1. Open Settings (gear icon).
2. Change theme to **Dark**, keyboard mode to **Vim**, click "Bundle
   images as a zip", switch dialect to **Obsidian**.
3. Hard-reload (Cmd+Shift+R).
4. **Pass:** all four settings persist.

### §16.2 — Cross-browser persistence

5. Open the app in a DIFFERENT browser (Chrome ↔ Safari, or incognito).
6. **Pass:** second browser opens with the same theme/mode/bundle
   settings.

### §16.3 — On-disk inspection

```bash
cat ~/.claude-explorer/preferences.json | python3 -m json.tool | head -30
```

Should show `{"version": ..., "data": {"theme": "dark", ...}}`.

---

## §17 — Permanent attachment caches (3 min)

Two caches:
- `~/.claude-explorer/cc-images/<conv>/<sess>--<N>.<sha8>.png` — CC
  image markers copied at fetch time.
- `~/.claude-explorer/files/<conv>/<file>/{thumbnail|preview|original|document}`
  — Desktop attachments.

### §17.1 — CC image survives rotation

1. Open a Claude Code conversation that has an image marker. Image
   renders.
2. Find the source path in the marker (DOM inspector, `<img>` `src`).
3. Delete the original from `~/.claude/image-cache/<sess>/<N>.png`.
4. Hard-reload.
5. **Pass:** image still renders (resolved from
   `~/.claude-explorer/cc-images/`).

### §17.2 — Desktop attachments cached

6. Open a Desktop conversation with an image OR non-image attachment.
7. `ls ~/.claude-explorer/files/<conv>/<file>/`. Should contain
   `thumbnail`, `preview`, `original`, or `document`.

### §17.3 — Friendly fallback copy

8. Find a CC image marker pointing at a path that doesn't exist on
   disk anywhere.
9. **Pass:** dashed-border tile with `ImageOff` icon and *"Image not
   in cache: <filename>"*. Hover tooltip: *"Original was rotated by
   Claude Code; this image was not present at fetch time, so we
   couldn't cache it."*

---

## §18 — PDF export with images (2 min)

1. Open a conversation with at least 2 images.
2. Click **PDF** in the conversation header.
3. Open the downloaded PDF.
4. **Pass:** images appear inline, not blank placeholders.

For deeper check:

```bash
uv run python -c "
import pypdf, sys
r = pypdf.PdfReader(sys.argv[1])
for p in r.pages:
    for x in p.images:
        print(x.name, len(x.data), 'bytes')
" ~/Downloads/<your-export>.pdf
```

Should list at least one image stream per image.

---

## §19 — Markdown export dialog (2 min)

1. Click **Markdown** in the conversation header.
2. **Pass:** dialog "Markdown export" appears with three radios:
   - **Inline** — single .md, all images as data: URLs.
   - **Bundle CommonMark** — zip with `images/`, `attachments/`,
     `conversation.md` using `[name](path)` links.
   - **Bundle Obsidian** — same shape but `[[path]]` wikilinks.
3. Select **Bundle Obsidian**. Tick **Save as default**. Download.
4. Reopen Markdown for any conversation.
5. **Pass:** dialog opens with Bundle Obsidian pre-selected (saved
   default).

### §19.1 — Inspect bundle

```bash
unzip -l ~/Downloads/<your-bundle>.zip
```

Should contain `conversation.md` + `images/` + `attachments/`.

### §19.2 — Cross-dialect spot-check

Run twice (CommonMark, then Obsidian). Compare `conversation.md`:
- CommonMark: `[spec.pdf](attachments/spec.pdf)`
- Obsidian: `[[attachments/spec.pdf]]`

---

## §20 — Search-pin + Cmd+G→Enter regression (2 min)

1. Open any conversation. Click **Search scope** → **Pin this
   conversation**.
2. URL ends in `?pin=conv:<uuid>`.
3. Cmd+F to open search panel.
4. **Pass:** at the top of the search panel, a small chip shows the
   pinned conversation name with X to clear.

### §20.1 — Cmd+G → Enter focus

5. Type a query with matches in two different conversations.
6. Press **Cmd+G** — moves to next match (focus stays in input).
7. Press **Enter**.
8. **Pass:** focus moves to the matching message bubble; bubble gains
   a temporary yellow ring. If the next match is in a DIFFERENT
   conversation, URL changes and target bubble is focused.

---

## §21 — Tool placeholder hidden (2 min)

The string *"This block is not supported on your current device yet…"*
must be silently stripped from the viewer, Markdown export, AND PDF
export.

1. Find a CC session with a tool block.
2. Viewer: NO instance of "not supported on your current device".
3. Markdown (Inline) export → grep for "not supported" → 0 matches.
4. PDF export → visually scan for the message → not present.

### §21.1 — Fenced-block badge survives

5. Find a CC session with a tool_use block.
6. **Pass:** in the viewer, you see a friendly badge *"Tool call or
   artifact not captured in export"* inside a code block. NO "not
   supported" text leaks elsewhere.

---

## §22 — Sidebar dim + title search scope (2 min)

### §22.1 — Sidebar dim 40%

1. Pin any conversation.
2. **Pass:** sidebar rows OUTSIDE the pin scope visibly dim — should
   look noticeably faded (40% opacity).

### §22.2 — Title search scope

3. Click in the sidebar title-search input (top of sidebar).
4. **Pass:** placeholder reads *"Search titles and projects"*.
5. Type a token that matches a project path but no conversation title.
6. **Pass:** sessions in that project surface.
7. Type a token that ONLY appears in conversation summaries.
8. **Pass:** sessions DON'T surface.

---

## §23 — If something fails

Capture:

- The §-number from this plan.
- For Part A (today's V1 changes): timing + DOM evidence.
  - §3 (images): conversation UUID + image file_uuid from `<img src=>`.
  - §4 (connection dialog): timing relative to backend kill (when did
    dialog open? when did "Last error" show?).
  - §5 (search): query string + screenshot of where panel landed.
- For backend test failures: `--tb=short` output.
- For Playwright failures: trace from `frontend/test-results/`.
- For UI failures: DevTools console screenshot if there's a JS error.
- The git SHA: `git rev-parse --short HEAD`.

---

## Appendix — User-visible behaviors changed (V1 sweep, 2026-05-09 / 2026-05-10)

**2026-05-09:**

1. `/api/<org>/files/<file>/{thumbnail,preview}` falls back to local
   cache on upstream 404 (was: 404 propagated; now: 200 with cached
   bytes if present; new "no local cache" detail in the actual-404
   case). **Also fixes Markdown/PDF exports for the same files.**
2. ConnectionStatus dialog: no stale "Last error" while connecting;
   first retry waits 4s (was 2s); dialog suppressed until retry≥2.
3. Search panel: auto-focuses first match when results land, with
   yellow ring on the matched message and scroll-into-view.
4. Backend test sweep: 214 → 222 passing, 0 skipped on macOS (PDF
   tests work without env-var prefix or `--ignore`).
5. Playwright: 0 known flakes (verified 620/620 at `--repeat-each=2
   workers=4`).
6. Cmd+F always switches the right-pane tab to Search regardless of
   the persisted `rightPaneTab` preference.
7. CC image-cache watcher poll interval lowered 60s → 5s (data
   integrity > idle CPU).
8. NEW: `claude-explorer install-watcher` CLI installs a launchd job
   so the watcher runs continuously, independent of `claude-explorer
   serve`. One-time setup; documented in CLAUDE.md.

**2026-05-10:**

9. Backend lifespan auto-warms the CC image cache on startup
   (previously required `claude-explorer warm-cc-cache` CLI).
   Idempotent; logs a summary line at INFO. Disable with
   `CLAUDE_EXPLORER_DISABLE_CC_WARM=1`.
10. Frontend session-level image-failure tombstone: after 10 failed
    fetches per URL, the `<img>` is skipped on subsequent renders
    (fallback tile renders directly, no network). Reload clears.
11. Backend test sweep: 222 → 225 passing.
12. Vitest: 130 → 135 passing (5 pre-existing MessageBubble tool-block
    failures fixed; tests now flip `showToolCalls` via a tiny helper
    component to match the new `showToolCalls=false` default).