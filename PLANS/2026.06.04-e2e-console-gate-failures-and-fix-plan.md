# Handoff: e2e release-gate failure + session coding summary (2026-06-04)

> **From:** the *"Medium post creation"* session (`76fe578b`) — the one writing the Part 3 Medium article.
> **To:** the *"Claude Explorer functionality"* build session (`a70251a5`).
>
> Two parts:
> 1. A short summary of the code/infra I shipped this session, with commit + file references, so you can build on it.
> 2. The e2e release-gate failure analysis + recommended fix, for you to action. **This is pre-existing test debt unrelated to the Part 3 / Cowork work below.**

---

## Part 1 — Coding/infra shipped this session (reference)

All on `main` (pushed). Current `pyproject` version is **`1.0.2`**. **Nothing has published to PyPI yet** — both `v1.0.1` and `v1.0.2` release runs were *blocked by the test gate* (see Part 2); the publish step never ran.

| Commit | What | Key files |
|--------|------|-----------|
| `d0dfea22` | `fix(scripts)`: `check-article-formats.py` silently ignored file args (a nonexistent path still printed "OK / exit 0"). Now honors explicit paths; missing path → exit 2. Added a black-box CLI-contract test. | `scripts/check-article-formats.py`, `backend/tests/test_check_article_formats.py` (new) |
| `f0309b74` | `ci`: gate the PyPI release on the test suite. `test.yml` made reusable (`workflow_call`); `release.yml` added a `test` job (`uses: ./.github/workflows/test.yml`) and `build` now `needs: test`. Chain is **test → build → publish** (publish still behind the `pypi` reviewer environment). | `.github/workflows/test.yml`, `.github/workflows/release.yml` |
| `4309b5a7` | `fix(mcp)`: **the real feature fix** — `list_sessions` and `list_projects` silently ignored `source="CLAUDE_COWORK"` (fell back to `"all"`). Added `CLAUDE_COWORK` to both `Literal`s + both `if source in (...)` guards + both docstrings. Also fixed a **test-isolation leak**: the MCP test conftest never isolated the Cowork dir, so any `all`/`COWORK` query read the dev's *real* Cowork sessions — now pins `CLAUDE_DESKTOP_APP_DIR`. | `mcp_server/server.py` (~L406-408, ~L496-497, docstrings ~L382/492), `mcp_server/tests/test_list_sessions.py` (new `test_source_filter_claude_cowork_excludes_other_sources`), `mcp_server/tests/conftest.py` |
| `f0c381cd` | `ci`: run `mcp_server/tests` in CI (and thus the release gate) — they were **not** in `pyproject` `testpaths` (`backend/tests` + `fetcher/tests` only) nor in `test.yml`. Added a `pytest mcp_server/tests/` step. (Did **not** touch `testpaths` to avoid disturbing the CLAUDE.md baseline-count invariant.) | `.github/workflows/test.yml` |
| `1a992c90` | `fix(test)`: regenerate stale e2e desktop fixtures. The committed fixtures carried `"is_temporary": false`, which the generator no longer emits (field removed from the model; not in runtime code). 3 desktop JSONs lost that line. **This is the commit that unblocked the e2e job's freshness check and thereby exposed Part 2's failures.** | `backend/tests/fixtures/desktop/by-org/00000000-1111-2222-3333-444444444444/*.json` (3) |
| `178a25b0`, `876eb70f` | `chore(release)`: version bumps `1.0.0 → 1.0.1 → 1.0.2`. | `pyproject.toml` |

Article/doc commits this session (context, not code): `be670f50` (Part 3 twins + styleguide + README MCP install), `ff84c4f2` (Part 3 `source` line lists Cowork; pipeline/wording).

Tags: `v1.0.1` (dangling, never published), `v1.0.2` (points at `876eb70f`, gate-blocked, never published).

**The Cowork fix (`4309b5a7`) is validated by the Python suite** (backend + fetcher + mcp_server pytest — all green in CI). It does **not** touch the FastAPI backend or the frontend.

---

## Part 2 — E2E release-gate failure: analysis + recommended fix

### Symptom
The `v1.0.2` Release run (<https://github.com/rpeck/claude-explorer/actions/runs/26978874381>) failed the gate on the **Playwright (fixture mode)** job: **54 failed / 386 passed / 1 flaky**. The Python job passed. Publish never ran. Almost every failure is the same: `Error: Console errors during test (see TESTING.md §5.15)` with the body being many `Failed to load resource: the server responded with a status of 500 (Internal Server Error)` (~929 total).

### Why it surfaced now (it's pre-existing, since ~June 2)
`main`'s **Tests** workflow has failed on every push since 2026-06-02. The older runs died fast on the e2e job's **"Verify fixtures are up to date"** step (the stale `is_temporary` fixtures), which `exit 1`s *before* the Playwright tests run — so the e2e tests hadn't actually run on `main` in days. Commit `1a992c90` fixed the freshness check, the Playwright tests finally ran, and these §5.15 console-error failures appeared. They are **not** caused by the Cowork fix or the fixture regeneration (the e2e tests use in-memory `makeSummary` mocks, not `backend/tests/fixtures`).

### Root cause
- The e2e webServer is **Vite only** (`frontend/playwright.config.ts` → `webServer.command: 'npm run dev'`, i.e. `vite`). There is **no backend**. `frontend/vite.config.*` proxies `/api` → `http://localhost:8765`, which isn't running, so any un-intercepted `/api/*` request returns **500** from Vite's dead proxy.
- Tests rely on `mockBackend()` in `frontend/e2e/fixtures.ts` to `page.route('**/api/...')`-intercept everything. There's a fail-loud catch-all `page.route('**/api/**', …→ 500)` (~`fixtures.ts:419`) so any *un-mocked* endpoint also 500s.
- The §5.15 console-error capture is an **`{ auto: true }`** fixture (~`fixtures.ts:760-790`) that records every `console`/`pageerror` and **throws after the test** if any error was seen (the throw is at ~`fixtures.ts:776`). So a single startup 500 fails the whole test.
- The app makes these startup calls on load: `/api/config`, `/api/orgs`, `/api/bookmarks`, `/api/preferences`, `/api/conversations`, `/api/health/watcher`. When any of them fire **before/outside** an installed route, they 500.

### Three distinct failure modes (don't assume one fix)
1. **Navigate-before-mock.** `beforeEach` calls `page.goto('/')`/`reload()` *before* `mockBackend()` (which runs in the test body). e.g. `e2e/keyboard-shortcuts.spec.ts:289`, `e2e/article-coverage-gaps.spec.ts` (the `Alt+<`/`Alt+>` describe). The startup calls during that navigation hit the dead proxy.
2. **Coverage gap even when mocked first.** `e2e/theme.spec.ts:4` and `e2e/settings.spec.ts:4` call `mockBackend()` *before* navigating and **still fail** — so the app is hitting an endpoint `mockBackend()` (no-arg defaults) doesn't install → catch-all 500. Likely a recently-added startup endpoint (`/api/orgs` or `/api/health/watcher` from the Cowork-multi-org / watcher work).
3. **Inconsistent `mockBackend` call conventions.** Some specs call `mockBackend(page)` (page as positional arg — e.g. `e2e/url-navigation.spec.ts:145`, `e2e/compact-markers.spec.ts:150`); others `mockBackend({ conversations, details })` (e.g. `e2e/keyboard-shortcuts.spec.ts:298`); others `mockBackend()`. These are not the same signature — at least one set is calling it wrong. Verify the actual `MockBackendFn` signature in `fixtures.ts` (~L89 type, ~L382 fixture).

### Affected specs (~30; from run 26978874381)
`url-navigation`, `sidebar-filters`, `compact-markers`, `redownload-conversation`, `cowork-multi-org`, `bookmarks`, `keyboard-shortcuts`, `fixtures-defaults`, `search-compact-auto-expand`, `connection-status`, `keyboard-navigation`, `spec-filters-modal-atom-editor`, `per-bubble-tools`, `per-block-copy-hover`, `image-attachments`, `conversations`, `search-pin-scope`, `article-coverage-gaps`, `theme`, `spec-filters-atom-behavior`, `spec-conversation-list-virtualized`, `spec-filters-group-combinator`, `search`, `markdown-export-dialog`, `spec-filters-modal-group-editor`, `spec-filters-migration-v1-v2`, `sidebar-behavior`, `settings`, `search-sidebar-scope-propagation`, `conversation-detail` (all under `frontend/e2e/`).

### Fix options considered
- **A — fixture-level baseline mock** (install default routes for the startup endpoints in an `{auto:true}` fixture before any navigation). Fixes all 54 in one place, but **adds more mocking** and touches shared infra used by all 113 specs. **The author (rpeck) is against broad mocking and rejected this.**
- **B — per-spec** (move `mockBackend(...)` before the `beforeEach` navigation, or replace `goto('/')`+`setItem`+`reload` with `page.addInitScript(() => localStorage.setItem(...))` to avoid the premature navigation entirely). Lower blast radius, but ~30 specs and modes 2–3 would mean *adding* mock coverage — again, more mocking.
- **C — RECOMMENDED (matches the author's preference: minimize mocks).** Stop intercepting `/api` with `page.route` and instead run a **real backend in fixture mode** as the e2e webServer (the project already supports a fixture-mode backend — see `mcp_server/tests/conftest.py` isolation and `scripts/generate_e2e_fixtures.py` which writes `backend/tests/fixtures/`). Point `playwright.config.ts` `webServer` at a backend started against those fixtures (e.g. `CLAUDE_EXPLORER_DATA_DIR=backend/tests/fixtures/... uvicorn backend.main:app`), drop most of `mockBackend()`, and keep `page.route` only for *targeted* cases (forced errors, SSE, image-404 paths). This is a real e2e rearchitecture but removes the whole class of "un-mocked endpoint → 500" failures and the §5.15 noise, and aligns with "minimize mocks to targeted cases."

### Key files
- `frontend/e2e/fixtures.ts` — `mockBackend` fixture (~L382-720), catch-all 500 (~L419-425), per-endpoint routes (config L460, orgs L471, preferences L483, bookmarks L541, health/watcher L445, search L514, …), §5.15 console capture + throw (~L760-790).
- `frontend/playwright.config.ts` — `webServer.command: 'npm run dev'` (~L50-52).
- `frontend/vite.config.*` — `/api` proxy → `:8765` (~L16-18).
- `.github/workflows/test.yml` — the `e2e` job (`Verify fixtures are up to date` step runs `scripts/generate_e2e_fixtures.py` + `git diff`; then `npx playwright test`).
- `TESTING.md` §5.15 — the console-error rule these failures trip.
- `scripts/generate_e2e_fixtures.py` — the deterministic fixture generator (source of truth for `backend/tests/fixtures/`).

### To unblock the release independently of this
The Cowork fix is fully covered by the Python suite. The 76fe578b session's recommendation to the author was to **scope the release gate to the Python suites** (backend + fetcher + mcp_server) so `1.0.2` can ship, and treat this e2e overhaul as its own task (option C). As of this writing the author has not chosen that; they want this e2e work done in the `a70251a5` build session.

### First steps for whoever picks this up
1. Reproduce locally: `cd frontend && npx playwright test theme.spec.ts --reporter=line` (a mode-2 case — mocks first, still 500s) and `keyboard-shortcuts.spec.ts` (mode-1). Iteration loop is ~12 min for the full suite, so work spec-by-spec.
2. Confirm the `MockBackendFn` signature and reconcile the `mockBackend(page)` vs `mockBackend({...})` callers.
3. Decide A/B/C with the author. If C, prototype the fixture-mode webServer on one spec first.

---

### Part 1 appendix — message UUIDs for future mining

Index of the substantive coding/debugging exchanges in session `76fe578b-7872-4263-bc24-f911c7f2efcc` ("Medium post creation"), so future Medium parts can pull the receipts without re-walking the full 8052-message outline. Organized by topic; each entry is one message UUID + a one-line description of what's in it. Sourced from `mcp__claude-sessions__get_session_outline` (assistant turns are the diagnostic/code content; human turns are the prompts that triggered them — both included where the prompt itself carries signal).

#### Topic A — `check-article-formats.py` silent-arg bug (diagnosis, TDD fix, test)

The script accepted file arguments and silently scanned its default glob anyway, so a nonexistent path returned `OK / exit 0`. TDD'd a CLI-contract test, then fixed `main()`. Shipped as commit `d0dfea22`.

- `448962c3-f7b4-437a-a644-6db10b528f8a` (human) — "Run check-article-formats.py on both twins"
- `62a56a1e-354d-472f-ba0b-5f812eace2d5` (assistant) — first "passed" claim, with the smell-test that triggered verification
- `3ae2775e-dfc6-41a5-a231-9396b717f0a4` (assistant) — confirms the script ignores arguments and scans `articles/*.md` regardless
- `cc3fb8a4-d7e9-4c8e-8e87-d30a694eceaf` (assistant) — full accurate picture: `main()` globs `articles/*.md` and ignores argv
- `34a382eb-97c4-4832-a15f-c8985501d9bc` (human) — "Fix the script. You should have caught this earlier, since you're supposed to use TDD!"
- `18e03f53-0da6-4ee8-b191-364fe77c61d4` (assistant) — acknowledges the footgun shape (accepts argv, ignores it, false-green on bad path)
- `2575018d-95af-4057-b810-bf6132b4b030` (assistant) — locates test home (no existing test; `backend/tests` + `fetcher/tests` only), proposes black-box CLI-contract test
- `99c8fc3f-1e68-4262-8831-9d4312cbf2c5` (assistant) — runs new test against UNFIXED script (red on the two bug cases)
- `33296b75-d40a-4d63-9584-b0d4753b4303` (assistant) — confirms red exactly where bug is; baselines green
- `01894337-0f89-430b-8b68-21adfec449a7` (assistant) — runs test against FIXED script
- `072b35e1-56b4-4a6c-b949-8ef666a5c1a2` (assistant) — all 4 green; live CLI sanity checks
- `6390abdd-06df-4acd-8c97-8177b42ee76f` (assistant) — collection moved by exactly the new file count (CLAUDE.md baseline-count invariant)
- `bafe03cd-3439-4fb9-9337-58d146a0f356` (assistant) — final accounting: bug, fix, test, lesson captured in MEMORY

#### Topic B — Test-gate CI wiring (`test.yml` → `workflow_call` + `release.yml` `needs: test`)

Made `test.yml` reusable and added a `test` gate job in `release.yml` so `build` and `publish` only run if tests pass. Shipped as commit `f0309b74`. Chain is **test → build → publish** (publish still behind the `pypi` reviewer environment).

- `01a7c9b9-…` (not in outline — see context) — author asks "what will I have to do to push a release?"
- `01b7c09d-4381-40b9-a9cc-7aecc42266ab` (human) — "Yes, add the test gate to release.yml; what will I have to do to push a release?"
- `a470538e-be90-435c-a491-62047473ba25` (assistant) — DRY-justification: make `test.yml` reusable rather than hand-copy a subset
- `67e7b917-2806-4fb4-8df4-328b3220ebe8` (assistant) — proposes the two-edit shape: `workflow_call:` on `test.yml`, `test` job in `release.yml` with `needs: test` chain
- `2987de44-ef26-4727-acc8-1c48e3213e30` (assistant) — `security-guidance` pre-tool hook blocked the workflow edit; injection-safe analysis, retry plan
- `fc4ecf80-cb03-42d0-99e7-0086d664548a` (assistant) — `test.yml` applied; pivots to `release.yml`
- `5df0ef40-f7d7-4b24-89cf-8c2466c4bef4` (assistant) — must Read `release.yml` first (had only viewed via `sed`)
- `7e820872-c437-4612-8a67-2026c59b3ace` (assistant) — second hook fire; explains injection-safety reasoning
- `671e9c13-6f3c-4ec8-ab7c-8decdbc645a2` (assistant) — both workflows applied; YAML validation
- `3216bb9a-1626-41b7-823a-f9fe4b4b2818` (assistant) — `yaml` module not in system Python; validates with ruby (built into macOS) — handy trick
- `e0f6aca4-5d6c-41b0-94ac-61751e9140b6` (assistant) — final summary: `workflow_call:` added, gate wired, YAML valid

#### Topic C — MCP `list_sessions` / `list_projects` Cowork source-filter bug (root cause, TDD, test-isolation leak)

The Literal types and `if source in (...)` guards on the two MCP tools only matched `CLAUDE_AI` / `CLAUDE_CODE`, so `source="CLAUDE_COWORK"` silently fell through to `"all"`. Adding the Cowork test exposed a second, latent bug: the MCP test conftest never isolated the Cowork dir (`CLAUDE_DESKTOP_APP_DIR`), so any `all`/COWORK query read the dev's REAL Cowork sessions. Shipped as commit `4309b5a7`.

- `062b6e57-7ac6-4186-8bd2-e1ef78f4e52a` (human) — "You mention the `source` filters for CC and Claude Desktop, but left out Cowork." — surfaced the bug
- `da0d6749-ce53-4411-b142-7cdf735f2cc4` (assistant) — verifies whether the Cowork value is even accepted by the MCP filter
- `25d35adc-b3b4-47c3-8c5a-d03a437dc97b` (assistant) — confirms three-source data model (`CLAUDE_AI`/`CLAUDE_CODE`/`CLAUDE_COWORK`); calls out it's a real bug, not an article omission
- `6533c164-8cb4-458a-8222-5a0ab2ee1e04` (assistant) — pinpoints `mcp_server/server.py:407` — only matches `("CLAUDE_AI", "CLAUDE_CODE")`, falls through to `"all"`
- `22f2da1a-e7fd-4858-a7c8-493b88b61e4b` (human) — "Yes, fix the Cowork filter and then the article"
- `6660a3ae-357c-4721-9cbd-4ad8fc0e7150` (assistant) — TDD plan; reads both filter functions and existing `source` tests
- `1614cb3c-82a8-46f1-8a7a-0ebbcd288ff0` (assistant) — locates test home: `mcp_server/tests/`; `store.list_conversations` already accepts COWORK at `backend/store.py:579` (the MCP wrapper is the only broken layer)
- `3789050d-f19e-4824-820e-738d94c65172` (assistant) — `conftest` has `add_desktop_session`/`add_cc_session` but no Cowork helper; chooses cleanest TDD entry
- `43431fb4-45bf-4e4b-a8ba-58a822c545cd` (assistant) — finds existing source-filter `_call` test pattern to model on
- `ea3de8df-2b80-4888-ae44-590061c6945c` (assistant) — adds the failing test
- `9d3c211e-2744-4655-b3f6-4dc4c549bf53` (assistant) — **TEST-ISOLATION LEAK FINDING**: red but `total: 52`, not 2; MCP fixtures isolate desktop dir but not Cowork dir
- `dee8feb0-3471-47bf-b05b-2f6435124275` (assistant) — locates `Settings` field with Cowork override (`config.py:151-155`)
- `82d4ffcd-58ed-442b-97d4-df96585a1f68` (assistant) — confirms Cowork dir is overridden by `CLAUDE_DESKTOP_APP_DIR`; conftest never pinned it
- `c5bd3a31-4508-4263-93ff-1d9ce9d24a74` (assistant) — clean red after isolation fix: `total: 2`; other 3 source tests still pass; pivots to code fix
- `ac160134-ddf8-4e13-9db7-ad47d8b318c3` (assistant) — all 65 mcp_server tests pass with the fix
- `48bbfdb1-a271-446e-9848-8507aba78df5` (assistant) — final summary: Literal + guard + docstring on both `list_sessions` and `list_projects`; conftest isolation added

#### Topic D — Adding `mcp_server/tests` to CI without disturbing the baseline-count invariant

Adds an explicit `pytest mcp_server/tests/` step in `test.yml`. Does NOT touch `pyproject` `testpaths` (`backend/tests` + `fetcher/tests` only) so the CLAUDE.md baseline-count invariant for backend pytest stays valid. Shipped as commit `f0c381cd`.

- `3e461008-5ff1-4ea7-8a59-98ff13ef486e` (human) — "Yes, fold in CI and cut 1.0.1"
- `ad62c237-23d4-42cf-a7ed-ed0642da7e9f` (assistant) — plan: add `test.yml` step (security hook will fire); won't touch `testpaths`
- `ba7984f9-f2db-457c-9731-5ad0c254bc5d` (assistant) — `test.yml` step added; pivots to job name + version bump
- `eda651f6-fb3c-4e21-9179-1616340e3698` (assistant) — workflow YAML validation + pre-push secret scan on new code files before committing
- `3df7b925-43f7-41bd-a56f-6070c11f3e2c` (assistant) — `/Users/me/` hits in test fixtures are pre-existing synthetic data (known-OK category)
- `952a1417-a394-4593-a7aa-ce817d56228b` (assistant) — main pushed; cutting `v1.0.1` tag triggers gated release

#### Topic E — Stale e2e desktop fixtures (`is_temporary` removal) diagnosis + regeneration

The `v1.0.1` release run failed at the e2e job's "Verify fixtures are up to date" step. The committed `backend/tests/fixtures/desktop/by-org/.../*.json` (3 files) carried `"is_temporary": false`, which the current generator no longer emits. Confirmed safe (no backend/fetcher test references; not in runtime code). Regenerated. Shipped as commit `1a992c90`. **This is the commit that unblocked the e2e job's freshness check and thereby exposed the Part-2 §5.15 console-error failures.**

- `e0e75220-860b-452d-8f8c-25bf460e777c` (human) — image of the failed v1.0.1 Actions run
- `b206a20f-e31c-4096-81e7-76fba31854f2` (human) — same image, attachment form
- `f9224e31-d35e-4be2-8e73-a8a391d42679` (assistant) — gate did its job (blocked publish); Playwright "Fixtures are out of date" is the real failure
- `7fd1d626-1240-4c84-9d85-cdec7f896e3f` (assistant) — 3 desktop fixtures each lost exactly one line; predates this session's changes
- `39ad2517-49c8-48f9-8a62-a475114bc4fa` (assistant) — drift is the removed `is_temporary` field
- `51ecc7cb-ea4a-49ee-a294-5e5527b50c3a` (assistant) — verified safe: no backend/fetcher references; not in runtime code; safe to regenerate
- `1fe4bd81-dbe0-4460-9d9c-66434c47e7f5` (assistant) — fixtures regenerated, pushed; planning the tag re-point

#### Topic F — Version bumps + tag dance for 1.0.1 / 1.0.2

Two release attempts, both gate-blocked, neither published. `v1.0.1` was blocked first by stale fixtures (Topic E), then the safety guard refused to delete-and-re-push an already-published tag — so the team cut a fresh `v1.0.2` against the fixed tree instead of moving `v1.0.1`. `v1.0.2` then hit the §5.15 Playwright wall (Topic G). Shipped as commits `178a25b0` (v1.0.1) and `876eb70f` (v1.0.2).

- `f39e2bfb-2e1a-440b-bcab-d386473686f7` (assistant) — safety guard blocked deleting/re-pushing the already-pushed `v1.0.1` tag; rationale (moving a published tag rewrites release history)
- `dd6a4f14-c600-47e3-87a8-3da0e600a4d8` (human) — "A" (option A — cut a fresh tag instead)
- `e1f62b0c-d364-47b9-99f0-953a8f0e42d9` (assistant) — bumps `pyproject` to `1.0.2`
- `7f7b4afb-0520-41f8-8feb-22718dab7b30` (assistant) — `v1.0.2` tagged and pushed; release running on the fixed tree
- `c7e9bd1e-0ca8-4882-bc0c-8ceb268f56ac` (human) — "Failed again: https://…/runs/26978874381" (the run that exposed §5.15)

#### Topic G — E2E release-gate failure forensics (root cause, three failure modes, A/B/C analysis)

The most valuable span for the future Part 4+ article. Walks from "v1.0.2 also failed" → pulling GH Actions logs → discovering 929 startup 500s → tracing them to Vite proxying `/api` → `:8765` with no backend running → finding `mockBackend()`'s catch-all 500 in `fixtures.ts` → identifying the §5.15 `{auto:true}` console-error fixture that throws → realizing the failures predate this session ("main's CI has been failing on every push since June 2") → enumerating three distinct failure modes → presenting A/B/C → author picks B then redirects to "minimize mocks" → handoff doc written.

- `16190450-3338-4af9-8efb-853337960acb` (assistant) — pulls failed-run logs via `gh`
- `ecaa75dc-5780-4d17-9869-5f1ee431a8df` (assistant) — fixture check passed; Playwright tests are the new failure (gate worked again)
- `0e99f165-4c5f-4891-9abe-4931790c9ab6` (assistant) — pattern: §5.15 "Console errors during test" across dozens of specs
- `c1a9b067-e67f-4aed-a25d-8b2e3363b9d4` (assistant) — 929 startup 500s as root signal; first hypothesis is the `is_temporary` removal (later disproven)
- `f7995fd2-a414-40be-9846-4facf73c77ba` (assistant) — disproves the hypothesis: `is_temporary` isn't in backend code; this session didn't touch the FastAPI backend
- `0994c69a-419d-4920-b57a-d70a0598dfd0` (assistant) — **root cause located**: e2e webServer is Vite-only with mocked backend; `/api` proxy 500s any un-intercepted request
- `865e01cb-3986-4403-9fe0-3b4cf7de3dae` (assistant) — **key forensic finding**: `main`'s Tests workflow has 8 consecutive `failure` runs predating this session, just dying earlier (at the freshness check) so the Playwright failures were hidden
- `0561657a-e587-4613-b4a0-e7f4c43113d0` (assistant) — full picture: pre-existing mess on `main`, not the Cowork work; presents options to author
- `4a64b26c-6819-4802-b17a-43c0404a2b2f` (human) — "2" (initial pick of "fix the e2e suite so main goes green")
- `4db1aa49-9c65-4c4f-8b7d-f3f1826e1127` (assistant) — option 2 it is; starts identifying `/api` requests slipping past mocks
- `126b9200-31fb-476d-9574-729db768acf8` (assistant) — setup analysis: Vite-only, `mockBackend()` shape, where the 500s slip through
- `2c71c5dd-d220-4ba5-b72a-0a365d0c6fb3` (assistant) — **bug pattern #1 located**: `article-coverage-gaps.spec.ts` `beforeEach` calls `page.goto('/')` BEFORE `mockBackend()` in test body — startup calls hit the dead proxy
- `dd4246d0-dcd6-4373-a2a3-19d2178f0251` (assistant) — scope: 54 failed / 386 passed; not the whole suite, specific navigate-before-mock pattern
- `4a32c3f8-4a16-412b-a8c3-b8ab8fb5a7dc` (assistant) — 54 pre-existing failures, one shared cause, fully independent of the Cowork work
- `cb64bb7e-0a44-4446-b744-e46be9a9cb79` (human) — "B I am in general not in favor of mocks, so at least let's minimize their use to targeted cases."
- `b0b5f481-febe-4d27-90be-52018d6205ee` (assistant) — B accepted; per-spec fix mostly REMOVES premature navigation rather than adds mocks
- `624a867e-34e7-4ca2-b598-1a0bbe6ffb87` (assistant) — `awk` broken; pivots to Reading actual `beforeEach` blocks — esp. `theme`/`settings`/`conversations` which already take `mockBackend` (so why are they still failing?)
- `6294fa39-18c8-42a8-a8a8-f1c0e900b03f` (assistant) — **three-failure-modes finding**: mode 1 navigate-before-mock; mode 2 coverage-gap-even-when-mocked-first; mode 3 inconsistent `mockBackend` signatures
- `83fe42d9-c69a-4151-b6ed-52a6a989a573` (human) — "Write your analysis to a document in PLANS/. I'll have the Claude Explorer functionality session do the work."
- `b6fed278-c4de-4aba-8c9a-7eebb1f5d441` (assistant) — handoff-doc plan (Part 1 summary + Part 2 analysis)
- `6d33f420-0066-4e1c-a7b0-2ce5deb89849` (assistant) — verifies cited commit hashes against git before writing the doc
- `8ae52f95-7605-4f26-a482-54bc4624e74d` (assistant) — handoff doc finalized at this file's path, every reference verified

> Coverage note: assistant turns with empty `summary` (e.g. positions `7464`, `7484`, `7500`, etc.) are tool-only calls (reads/edits with no narrative text) — they're skipped here because the surrounding narrative turns already describe what those tool calls did. If a future article needs the exact tool inputs/outputs (the actual diffs, test code, fixture contents), retrieve those with `mcp__claude-sessions__get_messages` using `include_tool_calls: true` and `include_tool_results: true` against the human/assistant UUIDs above as anchors.
