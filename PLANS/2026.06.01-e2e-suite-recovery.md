# E2E Suite Recovery Plan (2026-06-01)

**Owner:** the Claude Explorer functionality session (NOT the article session that wrote this).
**Goal:** take the Playwright e2e suite from **26 failed / 414 passed / 1 flaky** to **all green**, without weakening the §5.15 console-assertion guardrail.

---

## How we got here (read first)

The e2e suite had been **non-functional**: 13 filter specs carried a duplicate `withNetRetry` import (a mechanical error from the `withNetRetry`-wrap commit) that threw `SyntaxError: Identifier 'withNetRetry' has already been declared` at parse time. Playwright never ran them, and the suite never reported green. Because the suite couldn't complete, a layer of pre-existing failures stayed hidden.

The article session already fixed three root causes (currently **uncommitted in the shared working tree** unless committed by the time you read this):

1. **Duplicate `withNetRetry` import** removed from 13 `*.spec.ts` files (dropped the redundant identifier from the `import { makeSummary, withNetRetry }` line). Cleared the parse errors.
2. **Missing `DialogDescription`** added to `frontend/src/components/filters/ManageFiltersModal.tsx` (Radix `DialogContent` had a `DialogTitle` but no description → ~58 `aria-describedby` console-warning failures). Cleared them.
3. **Misplaced paren** in `frontend/e2e/url-navigation.spec.ts:187` that dropped `&filterMode=regex` from the test URL. Fixed.

Those three fixes recovered **34** failures (the first full run was 60 failed). The tree is otherwise byte-identical to the pre-cleanup state, so **the 26 remaining failures are all pre-existing** and unrelated to the article/history-cleanup work. This plan addresses those 26 (+1 flaky).

**Prerequisite:** make sure the three fixes above are present in the working tree before you start (ideally commit them first — see "Commit discipline" at the end). Re-run `npx playwright test --reporter=line` from `frontend/` to reproduce the 26 failures as your baseline.

---

## The §5.15 contract (do not weaken it)

`frontend/e2e/fixtures.ts` (≈ lines 620-690) installs an **auto-fixture** that, for every test, captures `console.error` / `console.warning` / uncaught `pageerror` and **throws at teardown if any are non-empty**, modulo `PROJECT_CONSOLE_ALLOWLIST` plus a **per-test** allowlist:

```ts
test('legacy noisy thing', async ({ page, consoleAssertions }) => {
  consoleAssertions.allowlist.push(/known third-party warning/)
  // ...
})
```

This rule exists to catch genuinely-swallowed errors (the 2026-05-24 settings flash bug). **The fix for Category A below is NOT to blanket-allowlist `Failed to load resource`** — that would blind the guardrail. Each fix must allowlist only the *specific, expected* error the test deliberately provokes, and the test must still assert the user-visible error UI appears.

---

## Failure inventory (26 hard + 1 flaky)

### Category A — §5.15 console error from an EXPECTED network failure (23 failures)

Every one of these tests deliberately mocks a `404` / `401` / `504` / connection-refused and then asserts the app's error UI. The browser logs `Failed to load resource: …` (or `net::ERR_CONNECTION_REFUSED`) at the network layer **regardless of JS handling** — for `<img>` 404s especially, the app cannot suppress it. So the console line is expected and must be allowlisted per-test.

| # | Spec : line | Test | Expected console error to allowlist |
|---|---|---|---|
| 1 | cc-image-broken-fallback.spec.ts:43 | CcImageMarkerText friendly fallback on /api/cc-image 404 | `Failed to load resource…404` |
| 2 | cc-image-broken-fallback.spec.ts:80 | InlineImageBlock friendly fallback on img 404 | `…404` |
| 3 | cc-image-broken-fallback.spec.ts:108 | fallback tile cache-miss copy + tooltip | `…404` |
| 4 | cc-image-broken-fallback.spec.ts:142 | `<img>` auto-retries once before fallback | `…404` |
| 5 | cc-image-lightbox.spec.ts:111 | CC image marker opens lightbox not new tab | `…404` |
| 6 | connection-status.spec.ts:18 | connecting dialog when backend unavailable | `net::ERR_CONNECTION_REFUSED` |
| 7 | connection-status.spec.ts:33 | retry counter incrementing | `…ERR_CONNECTION_REFUSED` |
| 8 | connection-status.spec.ts:52 | Retry Now triggers immediate retry | `…ERR_CONNECTION_REFUSED` |
| 9 | connection-status.spec.ts:77 | Connection Failed dialog after max retries | `…ERR_CONNECTION_REFUSED` |
| 10 | connection-status.spec.ts:103 | Try Again restarts retry process | `…ERR_CONNECTION_REFUSED` |
| 11 | connection-status.spec.ts:123 | Dismiss closes the dialog | `…ERR_CONNECTION_REFUSED` |
| 12 | connection-status.spec.ts:141 | dialog closes when backend returns | `…ERR_CONNECTION_REFUSED` |
| 13 | connection-status.spec.ts:179 | spinning icon during attempts | `…ERR_CONNECTION_REFUSED` |
| 14 | connection-status.spec.ts:198 | no "Last error" while still connecting | `…ERR_CONNECTION_REFUSED` |
| 15 | fixtures-defaults.spec.ts:175 | GET /api/cc-image 404 by default | `…404` |
| 16 | fixtures-defaults.spec.ts:216 | GET /api/{org}/files/{uuid}/preview 404 by default | `…404` |
| 17 | fixtures-defaults.spec.ts:228 | GET /api/attachments/… 404 by default | `…404` |
| 18 | image-attachments.spec.ts:236 | 404 thumbnail → ImageOff placeholder + filename | `…404` |
| 19 | image-attachments.spec.ts:272 | `[Image: source: <path>]` renders as `<img>` | `…404` |
| 20 | pdf-export-toast.spec.ts:114 | 504 timeout → user-readable error toast | `…504` |
| 21 | redownload-conversation.spec.ts:119 | 404 → friendly detail (not raw JSON) | `…404` |
| 22 | redownload-conversation.spec.ts:137 | cross-workspace 404 → workspace explanation | `…404` |
| 23 | redownload-conversation.spec.ts:153 | 401 (session expired) → session-expired copy | `…401` |

### Category B — real assertion failures (3, need individual debugging)

| # | Spec : line | Test | Assertion that failed | First hypothesis |
|---|---|---|---|---|
| B1 | connection-status.spec.ts:215 | first retry waits ~4 seconds (V1 polish) | `expect(elapsed).toBeGreaterThanOrEqual(3000)` | Retry-backoff timing: either the backoff constant changed, or the test's clock measurement is off under the mock. Confirm the actual first-retry delay in `ConnectionStatus` against the test's `>= 3000` expectation. NOTE: this test ALSO needs the Category-A allowlist (it provokes ERR_CONNECTION_REFUSED). |
| B2 | compact-markers.spec.ts:170 | `]` / `[` navigate between compact markers | `toHaveAttribute('[data-compact-marker-active]')` | The active-compact-marker data attribute is not being set when navigating with `]`/`[`. Check the compact-marker nav handler and that the active marker gets `data-compact-marker-active`. Could be a real regression in `useBracketCompactNav` or the marker component. |
| B3 | spec-filters-delete.spec.ts:40 | Unreferenced atom: trash → confirm removes the row | `getByRole('dialog').getByText(/^DeleteMe$/)` toHaveCount | The delete-confirm dialog's body text / structure. Verify the confirm dialog renders the atom name and that the selector matches the current markup. (Only filter spec still failing after the a11y fix.) |

### Flaky (1, not a hard failure)

- `bookmarks.spec.ts:172` — "right pane has Search and Bookmarks tabs", `toBeVisible(getByRole('tablist'))`. Passed on retry in the full run. Likely a missing settle signal before the tablist assertion. Add a deterministic wait on the prior action's DOM signal (see TESTING.md on Playwright settle signals) rather than relying on the auto-retry.

---

## Fix approach

### Step 1 — Category A (the 23)

Two viable shapes; pick one and apply consistently:

**Option 1 (recommended): a tiny scoped helper.** Add to `frontend/e2e/fixtures.ts` (exported) something like:

```ts
// Allowlist a specific, expected network-layer console error for an
// error-path test. Keep the regex tight so §5.15 still catches
// UNexpected swallowed errors.
export const expectNetworkError = (
  consoleAssertions: ConsoleCapture,
  match: RegExp,
) => consoleAssertions.allowlist.push(match)
```

Then in each test, immediately after arranging the failing mock:

```ts
consoleAssertions.allowlist.push(/Failed to load resource: .*\b404\b/)         // image / fetch 404s
consoleAssertions.allowlist.push(/net::ERR_CONNECTION_REFUSED/)                  // connection-status
consoleAssertions.allowlist.push(/Failed to load resource: .*\b504\b/)          // pdf-export
consoleAssertions.allowlist.push(/Failed to load resource: .*\b401\b/)          // redownload session-expired
```

**Option 2: scope it at the mock.** If the mock-setup helpers (`mockBackend`, the cc-image/attachment 404 mocks) are shared, have the helper that installs a deliberate-failure route also push the matching allowlist entry, so any test using that mock inherits the allowlist. Cleaner if most of these go through a few shared mock helpers; check `fixtures.ts` / `fixtures-defaults` first.

**Hard rules for Step 1:**
- Allowlist ONLY the status the test provokes; never a bare `/Failed to load resource/`.
- Do NOT remove or weaken any existing user-visible assertion. Each of these tests must still assert its error UI (fallback tile, toast, dialog, friendly copy). The allowlist change is additive.
- If, while doing this, you find a test whose console error is NOT explained by its own mock (i.e. a genuinely swallowed error), STOP and treat it as a real bug — that is exactly what §5.15 is for.

### Step 2 — Category B (the 3)

Debug each in isolation (`npx playwright test e2e/<spec> --reporter=line`). For B1 also apply the Step-1 allowlist. For B2/B3 confirm whether the assertion is stale (markup/selector drift) or a real behavior regression; fix the product code if it's a real regression, the test if the contract changed intentionally. Cross-check B2 against `useBracketCompactNav` and the compact-marker component; B3 against `ManageFiltersModal`'s delete-confirm dialog.

### Step 3 — Flaky bookmarks

Add the missing settle signal before the tablist assertion in `bookmarks.spec.ts:172`. Verify by running that spec ~5× in a row.

---

## Verification

1. Each fixed spec passes in isolation: `npx playwright test e2e/<spec>.spec.ts --reporter=line`.
2. Full suite green: `cd frontend && npx playwright test --reporter=line` → `0 failed`. Capture the exact `N passed` line. **Do not pipe through `tail`** — the pipe masks Playwright's exit code (a `tail` exit 0 hid a real failure during the article session). Read the output file or let it print.
3. No regression in the green suites: `uv run pytest -q` (expect 1139 passed / 1 skipped) and `cd frontend && npm run test:run` (expect 538 passed).
4. Sanity: confirm the §5.15 guardrail still bites — temporarily introduce a stray `console.error('boom')` in one component, confirm a test fails, then revert.

---

## Commit discipline

- The other CC session shares this working tree. To avoid index races, commit each logical fix with `git commit --only -- <paths>` rather than `git add -A`.
- Suggested commits: (1) the three prerequisite fixes if still uncommitted [`fix(e2e): de-dupe withNetRetry imports`, `fix(a11y): add DialogDescription to ManageFiltersModal`, `fix(e2e): url-navigation regex param paren`]; (2) `test(e2e): allowlist expected network errors in error-path specs (§5.15)`; (3) one commit per Category-B fix with a message naming the root cause.
- No AI attribution lines in commit messages (project rule).

---

## Out of scope

Backend pytest and frontend vitest are already green and unaffected; no changes needed there. This plan is e2e-only.
