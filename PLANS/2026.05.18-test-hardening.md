# 2026-05-18 — Test hardening (Workstream C)

## Context

Test-suite audit by Explore agent at 2026-05-18 surfaced:

  * **CRITICAL: security gap** — `/api/attachments/<uuid>/<file>/<variant>`
    has no path-traversal tests. TESTING.md §5.9 requires
    explicit `../../../etc/passwd`, symlink, and permission tests on
    any route taking a path.
  * **CRITICAL: mocking violation** — `test_cc_image_watcher.py`
    mocks `backend._search_index` in 3 tests, violating "integration
    tests must hit real state."
  * **Parallel runs** — 519 backend tests; no pytest-xdist enabled.
    Local + CI feedback is slower than it needs to be.
  * **Fixture duplication** — 15+ search test files reinvent
    `make_conversation_with_title` builders. Extract to shared module.

## Tasks (DAG)

```
C4 ─┬─ C5    (legacy-fixture migration uses shared builders)
    │
C1  │   (independent — new security tests)
C2  │   (independent — watcher rewrite; uses real index)
C6  │   (independent — coverage gap fills)
    │
C3 ─┴─  (xdist last — needs the codebase stable)
```

| ID  | Task | Files | Depends |
|-----|------|-------|---------|
| C1  | Path-traversal security tests on `/api/attachments/<uuid>/<file>/<variant>`. Cover `../../../etc/passwd`, symlink-outside-root, non-readable file. All 4xx without leaking resolved path. Mirror checks on `/api/files`, `/api/images`. | NEW: `backend/tests/test_attachments_security.py` | — |
| C2  | Rewrite `test_cc_image_watcher.py` to drop the `_search_index` mock. Use `isolated_data_dir`, write real CC sessions, trigger a real drift pass, assert real search index reflects the new state. | `backend/tests/test_cc_image_watcher.py` (becomes `test_cc_watcher.py` after B3) | — |
| C3  | Enable `pytest-xdist` (`-n auto`) in `pyproject.toml`. Extend `isolated_data_dir` to read `os.environ["PYTEST_XDIST_WORKER"]` (e.g. `gw0`, `gw1`) and construct a per-worker base under `tmp_path / worker_id`. Audit any `~/.claude-explorer/` writes in conftest. | `pyproject.toml`, `backend/tests/conftest.py` | C1, C2, C4 land first |
| C4  | Extract `backend/tests/fixtures/builders.py` with `make_conversation_with_title(...)`, `make_cc_session(...)`, NEEDLE-token helpers. Replace ad-hoc inline builders in `test_search_*.py` (15+ files) in batches. | NEW: `backend/tests/fixtures/builders.py`. EDIT: search test files | — |
| C5  | Migrate remaining `temp_data_dir` users to `isolated_data_dir` (the P0 fixture that clears `lru_cache`). Grep for `temp_data_dir`, convert each. | grep across `backend/tests/` | C4 |
| C6  | Fill coverage gaps: (a) `/api/preferences` PATCH with extra fields → 422 (Pydantic `extra='forbid'`); (b) FTS5 build-failure recovery falls back to linear scan; (c) `/api/export/all/markdown` with 0 conversations returns a valid empty zip. | NEW or EDIT in `backend/tests/test_preferences.py`, `test_search_fallback.py`, `test_export_*.py` | — |

## TDD discipline

Each task starts RED:

- **C1**: write the path-traversal tests assuming the route handles
  them correctly; if the route is broken, the tests fail and surface
  a security bug to fix before V1.
- **C2**: write the new test against the real search index; the
  initial run should fail because the old mocked test was hiding a
  real-behavior gap.
- **C3**: enable xdist locally, fix any worker-collision crashes one
  at a time.
- **C4**: write tests that USE the shared builders before extracting
  them; force the API design.
- **C5**: convert one test at a time; run that test in isolation
  before the broader suite.
- **C6**: each coverage gap is a missing test — write the test,
  watch it fail, then fix the implementation if needed.

## Acceptance

- `uv run pytest backend/tests` green under `-n auto`.
- Zero `@pytest.mark.skip` or `@pytest.mark.xfail` decorators left
  unjustified (platform/binary skips OK).
- No `unittest.mock.patch("backend.search_index…")` or
  `…summary_cache…` patches anywhere in test code.
- `test_attachments_security.py` covers all three traversal vectors.
- No AI attribution in commits.

## Execution

Dispatch via `llm-council-coding`. Recommended parallelism:
- C1, C2, C4, C6 in parallel from the start.
- C5 after C4.
- C3 LAST (needs the codebase + tests stable to avoid chasing flake
  from parallel execution + an in-flight refactor at the same time).
